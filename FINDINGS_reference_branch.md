# TTSR-lowlight 参考分支诊断报告

> 日期：2026-09-22/23
> 范围：参考图引导分支（LTE / SearchTransfer / RefCorrection / RefIllumTransfer / GlobalIllumHead）
> 所有数字均为本仓库实测，实验脚本见文末「复现方式」

---

## 0. 摘要

| 结论 | 数值 | 置信度 |
|---|---|---|
| 参考分支净收益（弱模型） | **+1.90 dB** | 高，15/15 张图受益 |
| 参考分支净收益（强微调模型 v81） | **+0.54 dB** | 中，单次测量 |
| oracle 对应替换收益（**当前权重 + 固定融合**） | **+0.45 ~ +0.60 dB** | 高，三个模型一致。**不是重训后的上限** |
| `tpl` 的净影响 | **−0.08 ~ −0.14 dB**（有害） | 中，配对设计 4/4 为负 |
| `tpl` 对匹配器的破坏 | 匹配率 **45.1% → 6.9%** | 高，有完美对照 |
| `RefIllumTransfer` 的总贡献 | **+0.91 ~ +1.20 dB** | 高，2 种子配对，15/15 受益 |
| └ 其中「参考内容」 | **+0.89 ~ +1.29 dB** | 高，配对训练对照 |
| └ 其中「额外修正能力」 | **≈ 0**（−0.09 / +0.02） | 高，常量参考对照 |
| `GlobalIllumHead` 的贡献 | **+0.001 dB** | 高，正常训练后仍为零 |
| 修复 `tpl`+`LTE` 的收益 | **+0.31 dB** | 高，2 种子配对一致 |

**一句话**：参考分支本身很值钱，但它的匹配器被 `tpl` 打坏了，而修复这件事只需要改两个配置项。

---

## 1. 背景

### 1.1 任务与数据

参考图引导的低光图像增强（reference-based low-light enhancement），**1:1 分辨率**，不是原始 TTSR 的 4× 超分。

数据：LOL v1 / LOLv2-real / LOLv2-synthetic / LSRW(data1)，均为成对 low/high，归一化到 `[-1, 1]`。

参考图来源在仓库里有两种，**分布不同**：

| 来源 | 实现 | 性质 |
|---|---|---|
| HR 随机裁剪 | `dataset/data1.py:171`、`lol.py:203`、`lolv2real.py:151` 的 `Ref = HR.copy()` | 训练期特权信息 |
| 生成参考 | `dataset/data1_nanobanana.py`，读 `nanobanana_ref/` | 可部署 |

评估时两者**并排混报**（`[LOL_gt]` vs `[LOL_nano]`），差距约 0.8~1.3 dB：

```
LOL           19.209 / 18.376   +0.83 dB
lolv2real     22.075 / 20.739   +1.34 dB
lolv2syn      18.362 / 17.482   +0.88 dB
data1-Huawei  20.486 / 19.491   +1.00 dB
data1-Nikon   17.368 / 16.546   +0.82 dB
```

**注意**：训练日志里的 `[LOL]` 评测**不是 oracle**——`--ref_degrade True` 会透传给 test loader（`dataset/dataloader.py:47` 的 `_test_args` 只在显式设 `--eval_ref_degrade` 时才覆盖），所以那个数字是**退化参考**下的结果。要看干净参考必须显式设 `--eval_ref_degrade False`。

### 1.2 模型结构

```
lr ──────────────┐
ref ──RefCorrection(可选)──┐
refsr ─RefCorrection───────┤
                           ▼
                         LTE ──> ref_lv1(64ch,H×W) ref_lv2(128ch,H/2) ref_lv3(256ch,H/4)
lrsr ──> LTE ─> lrsr_lv3 ─┐
refsr ─> LTE ─> refsr_lv3 ─┴─> SearchTransfer ─> S, T_lv3, T_lv2, T_lv1
                                                     │
lr ─> SFE ─┬─ Stage1(+T_lv3·σ(S)) ─┐                  │
           ├─ Stage2(+T_lv2·σ(S)) ─┼─ CSFI 跨流交换 ───┘
           └─ Stage3(+T_lv1·σ(S)) ─┘
                        MergeTail ─> sr
                        RefIllumTransfer ─> GlobalIllumHead ─> 输出
```

**LTE**：VGG19（ImageNet 预训练）`features` 前 12 层，切成 `range(2)/range(2,7)/range(7,12)`。实测输出为 `relu1_1` 64ch@H×W、`relu2_1` 128ch@H/2、`relu3_1` 256ch@H/4。
⚠️ 代码注释写的是 `relu1_2/relu2_2/relu3_4`，**与实际不符**（通道数正确，层名错位）。

**SearchTransfer**：
- 搜索只在 **lv3** 做：3×3 unfold + L2 归一化 + `bmm` 得余弦相似度矩阵 `R ∈ [N, Hr·Wr, H·W]`，对参考维取 `max` 得每个低光位置的最佳参考位置 `arg` 和相似度 `S`。
- `arg` 是**硬 argmax**，是整个匹配决策的全部内容。
- 迁移用**同一个 arg**，在三个尺度以 `(k,s,p) = (3,1,1)/(6,2,2)/(12,4,4)` unfold（kernel 与 stride 同步放大，保证三层列数都等于 lv3 格数），按 arg 取列，再 fold 回各层原生分辨率。

**MainNetEnhance**：删掉全部 PixelShuffle，三条流都在同一分辨率；CSFI 从"跨分辨率上下采样"改为"空洞卷积 d=1,3 的同分辨率融合"。

**两个光照模块**（刻意放在 `forward` 之外，供分块推理时对拼接后的整图施加一次）：
- `GlobalIllumHead`：整图 mean/std → 每通道 log_gain/bias/log_gamma，输出层零初始化。用 `low`，**不依赖参考图**。
- `RefIllumTransfer`：对 ref/low/当前输出各做 1/8 平均池化，拼 9 通道 → 3 层 3×3 → 低频残差。

### 1.3 训练目标

`rec`(L1, 1.0) + `per`(VGG relu5_1, 0.1) + `tpl`(迁移感知, 0.1) + `adv`(默认 0) + `illum_smooth`(1.0) + `color`(0.5) + `exposure`(1.0) + `illum_match`(默认 0)

配置默认值：`num_res_blocks='8+8+4+2'`（30 个 ResBlock）、`n_feats=64`、`res_scale=1.0`。

---

## 2. 本次新增的实验基础设施

| 开关 | 作用 | 默认 |
|---|---|---|
| `--seed` | 固定 torch/numpy/random + `cudnn.benchmark=False` | `-1`（关闭） |
| `--no_reference` | 关闭**所有**参考路径（纹理 + 参考光照 + 修正头） | `False` |
| `--oracle_matching {off,index,full}` | 诊断：用真值对应替换学习的 argmax | `off` |
| `--no_ref_illum` | 关闭 `RefIllumTransfer` 的贡献（模块仍构造，保证初始化逐位一致） | `False` |
| `--ref_illum_const_ref` | 对照：`RefIllumTransfer` 结构与容量不变，只把参考输入换成全零常量 | `False` |

**所有开关默认关闭，且逐一做过逐位回归验证**（用 `git worktree` 拉 HEAD 对比，5 个输出最大差 0.000e+00）。

**无参考模式的验证**：同一套权重下换掉参考图，输出差**恰好为 0**，证明参考路径被完全切断。

---

## 3. 发现

### 3.1 匹配质量：光照差异让同纹理匹配退化

**协议**：LOL eval15 的 low/high 逐像素对齐，所以"纹理相同的参考位置"就是同位置，匹配正确性有客观真值。5 张图、256×256 中心裁剪（lv3 网格 64×64 = 4096 候选），low 均值 0.080、high 0.475。

| 实验 | lv1 | lv2 | lv3（实际使用） |
|---|---|---|---|
| 控制组：low 配 low | 100.0% | 100.0% | 100.0% |
| 真实组：low 配 high | **5.2%** | **57.5%** | **61.3%** |
| 消融：去掉每个 patch 的均值 | 11.9% | 62.8% | 62.6% |

- 同位置余弦相似度从 **1.000 掉到 0.763**（最优错配 0.780）。
- **失败是"跑远"不是"偏一点"**：平均距离 7.50 格；**仅统计错匹配时 19.38 格 ≈ 78 像素**；放宽 ±1 格只多 3.5%。
- 正确位置的排名：top-1 61.3% / top-5 71.0% / top-10 74.8% / top-50 81.8%（共 4096）。
- **决策余量极小**：`top1−top2` 全部 0.0575、找对 0.0907、**找错 0.0049**；GT 余量找错时 **−0.0442**。
- **全局色调修正修不好**：提亮 query 最好 65.1%（`low^0.7`），过头降到 59.5%；压暗 reference 最好 63.5%。
- **层级差异巨大**：lv1 5.2% → lv3 61.3%，说明"在 lv3 搜索"这个设计是必要的。
- **反直觉现象未解释**：按低光局部亮度分层 75.4 / 73.5 / 61.7 / **34.5%**（暗→亮）；但与光照比值相关性只有 +0.021、与过曝率 −0.008、与低光亮度 −0.303。**亮区更差，但不是光照差距造成的。**

### 3.2 软门控 S 几乎恒定

S 是余弦相似度，理论上限 1，因此 `sigmoid(S)` 的**硬上限是 0.731**。实测：

```
最优余弦 0.464 ~ 0.975  →  sigmoid 0.614 ~ 0.726
```

宽度只有 0.11，而且 S 还要从 16×12 双三次插值到 H×W（进一步平滑）。**这个"软注意力"几乎不提供逐位置区分度，效果接近常数缩放。**

补充证据：把门控换成"诚实"的正确匹配相似度反而掉点（见 3.5）。

### 3.3 加权梯度审计

在 p10 checkpoint 上，分别算每个损失对三组参数的加权梯度范数：

| loss | 加权值 | grad\|RefCorrection | grad\|光照模块 | grad\|主干 |
|---|---|---|---|---|
| rec | 0.37033 | 5.591e-02 | 1.008e+00 | 9.231e-01 |
| per | 0.40791 | 1.023e-01 | 1.058e-01 | 1.168e+00 |
| **tpl** | 0.20200 | **5.287e-01** | 9.323e-02 | 1.246e+00 |
| ref_correct | 0.01092 | 1.125e-02 | None | None |
| color | 0.01836 | 6.496e-02 | 4.758e-01 | 8.123e-01 |
| exposure | 0.15275 | 5.677e-02 | 8.371e-01 | 8.736e-01 |
| illum_smooth | 0.00190 | 7.307e-03 | 5.603e-03 | 5.299e-02 |

**`tpl` 对 `RefCorrection` 的梯度是它专属监督 `ref_correct` 的 47 倍。**

这说明"这个头实际受纹理损失驱动"——它本该服务的监督信号在尺度上被压过去了。同时也可以排除"梯度路径被完全截断"这个担心（`detach` 只出现在 `else` 分支，有 `RefCorrection` 时梯度是通的，实测各损失对该模块的梯度都非 None）。

⚠️ **但要注意这只能说明影响尺度，不能说明作用方向**：两项梯度可能同向也可能冲突；单个 checkpoint 的测量也不代表整个训练轨迹。所以"修正头因此收敛到近恒等"目前是**假设而非结论**。

### 3.4 `tpl` 正在把匹配器打坏（最关键发现）

同一批 eval 图，只换特征提取器：

⚠️ **协议说明**：匹配正确率对**候选数量**敏感（lv3 网格越大，top-1 越难）。所以下表的两个协议并排给出，不能把不同协议的绝对数字直接当成前后变化。

| LTE | 协议 A：5 张 / 256×256 中心裁剪（4096 候选） | 协议 B：15 张 / 原尺寸 600×400（15000 候选） |
|---|---|---|
| ImageNet 预训练（未训练） | **61.3%** | **45.1%** |
| C 组（无参考，LTE 收不到梯度） | **61.3%** | **45.1%** ← 完美对照 |
| E 组（`tpl_w=0` + `freeze_lte`） | **61.3%** | **45.1%** |
| B 组（`tpl_w=0`，LTE 可训） | 58.3% | 40.2% |
| A1（`tpl_w=0.1`，seed 42） | 17.8% | **6.9%** |
| A2（`tpl_w=0.1`，seed 43） | — | **7.4%** |

**绝对值随候选数变化，但排序和损伤幅度在两种协议下完全一致。** C 组和 E 组精确等于未训练基线，证明这两个 run 里 LTE 一个梯度都没收到。

下表（含 `RefCorrection`）沿用协议 B：

| LTE 状态 | 匹配 top-1（协议 B） |
|---|---|
| 原始 ImageNet 预训练 | 45.1% |
| A1 的 LTE | 6.9% |
| A1 的 LTE + `RefCorrection` | 13.1% |
| B 的 LTE（`tpl_w=0`） | 40.2% |
| B 的 LTE + `RefCorrection` | 41.5% |

**表格标题务必区分"是否经过修正头"**：6.9% 和 13.1% 是同一组权重在加/不加 `RefCorrection` 下的两个数。

三个要点：

1. **C 组恰好 45.1%，一点没变**——无参考模式下 LTE 收不到梯度，就该等于预训练值。这是对测量方法本身的验证。
2. **`tpl` 是元凶**：关掉只掉 5 个点，开着掉 **38 个点**，两个种子一致。
3. **`RefCorrection` 是"损伤补偿器"**：6.9%→13.1%、7.4%→15.3%（而 B 组 40.2%→41.5%，没什么可修）。这也解释了 v79/v81 的专项实验测不出效果——它们用 `--freeze_lte True`，匹配器完好，头没有东西可修。

**机制**：`tpl` 拿匹配模块的输出 T 去监督"增强输出的 LTE 特征"，等于要求 LTE 表示"T 里有什么"而不是"这块纹理是什么"。**这是一个自我拆台的目标**：用匹配的输出去训练匹配所依赖的特征空间。

**影响范围**：

```
--freeze_lte True  : 101 个脚本
--freeze_lte False :  42 个脚本
不传(默认 False)    :  13 个脚本     → 合计 55/158 (35%) 的 run 会中招
```

### 3.5 oracle matching：当前权重的对应替换收益 ≈ +0.5 dB

用真值对应（identity，因为参考图是对齐的 HR）替换学习的 argmax：

| 模型 | 现状 | oracle 下标 | oracle 下标+S | 增益 |
|---|---|---|---|---|
| A1（LOL 30ep，匹配率 6.9%） | 17.453 | 18.053 | 17.980 | **+0.53 ~ +0.60** |
| B（LOL，tpl 关，匹配率 40.2%） | 17.590 | — | 18.039 | **+0.45** |
| v81（data1 微调，`freeze_lte=True`，50 张） | 18.726 | 19.298 | 19.210 | **+0.48 ~ +0.57** |

**三个模型、两个数据集、匹配率 7%~45%，oracle 对应替换的收益都是 +0.45~0.60 dB。**

⚠️ **这个数字不能读作"匹配研究的理论上限"。** 它实际测量的是：

> 固定现有权重和融合方式，在**对齐 HR 参考**下，把预测对应换成真实对应，输出改善多少。

它不等于重训后的上限，原因有三：
1. **下游网络已经适应了旧匹配**——只在推理时换对应，不等价于从训练开始就用更可靠的匹配；
2. **融合和门控仍然固定**——正确特征能否被充分利用还受接收端限制；
3. **参考条件固定在对齐 HR**——不代表生成参考条件下的边界。

准确的表述是：**当前 checkpoint 上，对应替换有约 +0.5 dB 的可测价值，说明匹配改进值得做，但重训后的收益上限尚未确定。** 工程上暂时限制复杂匹配算法的投入是合理的，理由是"目前测到的收益有限"，而不是"已证明最多只有 0.5 dB"。

**反直觉且三个模型一致**：`index`（只换 T）比 `full`（T 和 S 都换）**更好**，差 0.07~0.09 dB。原因：

```
正确匹配的平均余弦 0.1848   vs   最优匹配的平均余弦 0.3253
```

把 S 换成"诚实"值会让 `sigmoid(S)` 从 0.58 掉到 0.55，注入变少。**含义：相似度幅度不该被当作可信度。**

### 3.6 参考分支的组成分解

推理时逐条关闭路径，同一权重：

**A1**（LOL，从零 30ep，无参考基线 15.45 dB，15 张）

| 开启的路径 | PSNR | 相对无参考基线 |
|---|---|---|
| 全部开启 | 17.453 | +2.005 |
| 只有纹理迁移 | 16.600 | **+1.152** |
| 只有参考光照 | 16.316 | **+0.868** |
| 只有全局光照（=无参考基线） | 15.448 | 0 |
| 纯主干 | 15.447 | −0.001 |

**v81**（data1 微调，`freeze_lte=True`，50 张）

| 开启的路径 | PSNR | 相对无参考基线 |
|---|---|---|
| 全部开启 | 18.707 | +0.540 |
| 只有纹理迁移 | 18.719 | **+0.552** |
| 只有参考光照 | 18.158 | **−0.009** |
| 只有全局光照 | 18.167 | 0 |
| 纯主干 | 18.167 | 0.000 |

两条路径**近似可加**（A1: 1.152+0.868=2.020 vs 实测 2.005；v81: 0.552+(−0.009)=0.543 vs 实测 0.540）。

**交叉验证**：A1 推理时去掉参考得到 15.448，而**单独训练**的无参考模型 C 得到 15.549——两者几乎相同，说明推理消融与训练消融一致，A1 没有学到"离不开参考"的依赖。

### 3.7 训练消融：`RefIllumTransfer` 值 +1.1~1.6 dB（`tpl` 开启的配置下）

⚠️ **上一节 v81 的推理消融（−0.009）回答的是另一个问题。** 它准确回答了"这份 checkpoint 当前是否依赖这个模块"；而下面的训练消融回答的是"在这套训练条件下，允许模型使用这个模块，最终能否训练得更好"。两者不是同一个问题，也不在同一组训练条件下，所以不能互相否定。

**协议**：LOL 从零训练，2 init + 28 epoch，batch 8，crop 128，seed 42/43 配对，epoch20，oracle 参考，15 张 eval15。

| run | 配置 | PSNR | 匹配率 |
|---|---|---|---|
| A1_base_s42 | 基线（tpl 0.1，LTE 可训） | 17.453 | 13.1% |
| A2_base_s43 | 同上，seed 43 | 16.653 | 15.3% |
| B_notpl_s42 | ① 去掉 tpl | 17.590 | 41.5% |
| D42 / D43 | ② 关闭 RefIllumTransfer | 15.811 / 15.592 | 14.1% / — |
| E_fix_s42 | ③ tpl 关 + LTE 冻结 | **17.765** | **46.1%** |
| E_fix_s43 | 同上，seed 43 | **16.953** | **46.2%** |
| C_noref_s42 | 无参考基线 | 15.549 | — |

**配对结论**：

```
参考分支总收益 (A1 − C)        : +1.903 dB   (15/15 受益)
RefIllumTransfer (A1 − D42)    : +1.642 dB   (15/15 受益)
RefIllumTransfer (A2 − D43)    : +1.061 dB   (15/15 受益)
修复组合 (E42 − A1)            : +0.312 dB
修复组合 (E43 − A2)            : +0.300 dB
噪声 (A2 − A1)                : −0.800 dB
噪声 (E43 − E42)              : −0.812 dB
噪声 (D43 − D42)              : −0.219 dB
nano 参考设定 (A1 − C)         : +1.587 dB   (14/15 受益)
```

### 3.8 `RefIllumTransfer` 的学习率陷阱

为什么 v81 上它没效果？测它实际输出的修正量：

```
A1 (LOL 从零训练, lr=1e-4)      mean|delta| = 0.0496   ← 真的在工作
D42 (同配置关闭该模块)           mean|delta| = 0.00087  ← 停在初始值（应如此）
v81 (data1 微调)                mean|delta| = 0.00439  ← 也几乎停在初始值
```

原因在 `trainer.py:99-101`：

```python
lr_refillum = getattr(args, 'lr_rate_refillum', -1)
if lr_refillum is None or lr_refillum <= 0:
    lr_refillum = args.lr_rate        # ← 回退到「基础学习率」
```

v81 的 `--lr_rate` 是 **3e-7**，且**没有**显式设 `--lr_rate_refillum`，所以这个模块用 3e-7 训了 4 个 epoch，等于冻着。

**对比说明问题**：v81 显式给了 `--lr_rate_refhead 5e-6`（RefCorrection 拿到正常学习率），但 `refillum` 没有。

**影响范围**：

```
显式设了 --lr_rate_refillum 的脚本 :  8 / 158
基础 lr ≤ 2e-6 的脚本            : 89 / 158
```

**绝大多数微调实验里 `RefIllumTransfer` 都近乎冻结，它的价值一直没被测出来。**

### 3.9 `RefIllumTransfer` 的收益从哪里来：参考内容 vs 额外修正能力

`RefIllumTransfer` 的输入是 `[参考低频图, low 低频图, 当前输出低频图]`。**删掉整个模块，同时删掉了"参考信息"和"一份额外的低频修正能力"。** 所以要做一个只换参考内容的对照：

> 保留模块的全部结构、参数量、训练流程，只把它的**参考输入换成常量（全零图）**。纹理迁移分支仍然使用真实参考，否则又会混入另一条路径的变化。

**协议**：`tpl_w=0` + `freeze_lte=True`（即 3.7 的 E 配置），只多一个 `--ref_illum_const_ref True`，seed 42/43 配对。

| run | 参考输入给 `RefIllumTransfer` | PSNR |
|---|---|---|
| E_fix_s42 | 真实参考 | **17.765** |
| F_constref_s42 | 常量 | 16.480 |
| E_fix_s43 | 真实参考 | **16.953** |
| F_constref_s43 | 常量 | 16.062 |

再加上"在 F 的权重上推理时关掉模块"这一格，就能三段分解：

| | seed 42 | seed 43 |
|---|---|---|
| 真实参考 + 模块 | 17.765 | 16.953 |
| 常量参考 + 模块 | 16.480 | 16.062 |
| 常量参考，推理关掉模块 | 16.568 | 16.045 |
| **参考内容的贡献**（真实 − 常量） | **+1.285 dB**（15/15 受益） | **+0.891 dB**（14/15 受益） |
| **额外修正能力的贡献**（模块能否只靠 low+输出帮上忙） | **−0.088 dB** | **+0.017 dB** |

**结论：这个模块的收益 100% 来自参考内容。** 当它拿不到参考时，训练出来的模块相对"根本没有这个模块"既不更好也不更差（−0.09 / +0.02 dB）。因此不必担心"+1.1~1.6 dB 只是额外容量带来的"。

> 注：上表的 E 数值（17.765 / 16.953）与 3.7 节独立测得的完全一致，交叉验证通过。

### 3.10 `GlobalIllumHead` 当前几乎无贡献

与 `RefIllumTransfer` 相反，`GlobalIllumHead` 的零贡献是**可信**的：A1 的 `--lr_rate` 是 1e-4，`lr_rate_illum` 同样回退到基础 lr，所以它**被正常训练过**，仍然只贡献 **+0.001 dB**。

| 模块 | 被训练了吗 | 贡献 | 结论 |
|---|---|---|---|
| `GlobalIllumHead` | ✅（lr 1e-4） | +0.001 dB | **低优先级，可暂时关闭的简化候选** |
| `RefIllumTransfer` | ❌ 多数实验里被冻着 | +0.91~1.20 dB（其中参考内容 +0.89~1.29） | **有价值，要修学习率** |

措辞上"**在已测 checkpoint 中贡献约为零**"比"这个结构永远无用"更准确——本报告的样本是 2 个模型、1 个数据集。

### 3.11 fold 的归一化（已修复并优化）

原版 TTSR 用常数 `/(3*3)` 归一化 fold 结果，但每个像素被覆盖的次数实测是**角落 4 / 边缘 6 / 内部 9**（极差 **2.25×**），导致纹理边缘变暗：

```
T_lv3 / T_lv2 / T_lv1 误差 : 1.95 / 2.30 / 2.20  (原版 /9)
                              4.8e-07             (逐像素掩码)
```

本仓库已改用 `fold(ones)` 逐像素归一化（正确）。本次进一步把 `ones_like(T_lv*_unfold)` 换成 1 通道掩码 + 缓存：**数值逐位不变**，batch=8 时临时显存 **126 MB → 189 KB**。

### 3.12 其他发现

- **训练日志的 `[LOL]` 不是 oracle**：`ref_degrade` 透传给 test loader，那个数字是退化参考下的结果。我曾因此复现不出 18.069（实际用干净参考得 17.453）。
- **`p10-rgmsa` 的 checkpoint 装不进当前代码**：它含 `MainNet.illum_estimator.*` 和 `MainNet.rg_blocks.*`（Q/K/V 注意力 + pos_emb），这些是 **Retinexformer** 的组件（`/root/projects/Retinexformer-master/basicsr/models/archs/RetinexFormer_arch.py`），但**这段代码不在仓库里**。40 个近期实验里只有它一个如此，其余 18 个与当前代码一致。该实验目前无法用仓库代码复现。
- **`cudnn.deterministic=True` 让训练慢 7.6 倍**（160s → 21s/epoch）。
- **`res_scale` 是死参数**：全仓 150 处脚本一律为 1.0。
- **`CSFI2_SameRes` 的重复拼接**（`MainNetEnhance.py:90-91`）：`cat((x1, x21, x1))` 第三路是自己输入的重复。数学上 `Conv([x1,x21,x1]) = (W_a+W_c)x1 + W_b·x21 + b`，是**冗余参数化**，不是 identity skip；可折叠成两路输入且函数不变。
- **`exposure` 先验与真值冲突**：把真值 GT 喂进损失，LOL `exposure=0.2168`（对 GT 的梯度范数 2.42e-04）、data1 `0.1715`，而 `exposure_w=1.0` 与 `rec_w` 同权重。`color` 冲突小（0.0174 / 0.00307）。
- **末层零初始化不会永久阻断前层**（实测：第 0 步 `conv1.grad=0`、`conv3.grad=2.9e-3`；一次更新后 `conv1.grad=5.1e-5`）。仓库 docstring 里"dL/dconv1==0 所以学不动"的**推理是错的**（虽然它声称的实测现象可能是真的）。
- **`apply_illum_head` 的 clamp 让初始化不是严格恒等**：`x < -0.98` 的最暗像素会被抬到 −0.98。

---

## 4. 结论与行动建议

### 4.1 行动清单（按投入产出比排序）

| 优先级 | 动作 | 依据 | 预期 |
|---|---|---|---|
| **1** | `--freeze_lte True` 或 `lr_rate_lte → 0` | `tpl` 把匹配率从 45% 打到 7%；55/158 脚本没冻结 | 保护匹配器（新训练下 +0.175 dB，见 4.2） |
| **2** | 降低/去掉 `tpl_w` | 配对 4/4 为负；且是匹配器损坏的根因 | +0.137 dB + 保护匹配器 |
| **3** | 给 `RefIllumTransfer` 显式设 `--lr_rate_refillum`（1e-5~1e-4） | 它值 +0.91~1.20 dB（**其中参考内容 +0.89~1.29 dB**），但只有 8/158 脚本设了 | 解锁 0.9~1.2 dB（微调场景待验证） |
| **4** | `GlobalIllumHead` 列为低优先级、可暂时关闭的简化候选 | 已测 checkpoint 上贡献 +0.001 dB | 简化，几乎零损失 |
| **5** | 暂缓复杂的匹配算法改造 | 三个模型上 oracle 对应替换的收益 0.45~0.60 dB（**仅为当前权重下的可测价值**） | 避免在收益不明时过度投入 |
| 6 | 评估时显式区分 oracle / 生成参考 | 两者差 0.8~1.3 dB | 避免误读 |
| 7 | 复核 `exposure_w`（真值自己违反该先验） | GT 上 exposure=0.217 | 待验证 |
| 8 | 门控 S 与相似度幅度解耦 | `index > full` 三模型一致 | 待验证 |
| 9 | 暂时**不要**同时改 CSFI / top-k / 门控映射 / 曝光损失 | 否则无法判断收益来源 | 保持结构不变，一次只动一个 |

**第 1 和第 3 项预期收益最大**：一个只改配置，一个只改 option 回退逻辑，都不需要架构改动。

**关于 Retinexformer 的接入顺序**：本报告说明当前 TTSR-lowlight 存在明确的**训练层面**问题。合理的比较顺序是先得到修复后的结构基线，再与无参考 Retinexformer 比基础增强与跨域表现，最后才有依据判断参考分支的增量。直接换主干会把"训练配置问题"和"结构问题"混在一起。

另外 `p10-rgmsa` 虽含 Retinexformer 组件，但**对应实现不在仓库中**——这条历史分支需要先恢复代码与配置的可复现性，暂时不能当作"已经试过 Retinexformer"的可靠对照。

### 4.2 已验证的修复组合

```
tpl_w 0 + freeze_lte True
  seed42: 17.765  vs 基线 17.453  →  +0.312 dB
  seed43: 16.953  vs 基线 16.653  →  +0.300 dB
  匹配率: 13.1%/15.3%  →  46.1%/46.2%
```

两个种子一致 +0.31 dB，匹配率恢复到预训练水平。注意**不能**把 +0.31 dB 与 3.5 节的 oracle 增益相除来解释：oracle 实验改的是**推理时的对应关系**，而这里改的是**训练目标和 LTE 是否可训**，两者不是同一个干预。

**收益归因（seed 42）**：

```
关闭 tpl            (B  − A1) : +0.137 dB
再冻结 LTE          (E  − B ) : +0.175 dB
------------------------------------------
合计                (E  − A1) : +0.312 dB
```

所以 **+0.312 dB 是组合收益，不能读成"单独冻结 LTE 就有 +0.3 dB"**。目前缺少"`freeze_lte=True` + `tpl_w=0.1`"这一格，因此无法完整区分 `tpl` 对输出的**直接影响**和它通过训练 LTE 产生的**间接影响**。

⚠️ **另一个容易踩的坑：冻结不会修复已经损坏的 LTE。**

```
加载已被训练损坏的 LTE  →  freeze_lte=True  →  只是把损坏状态固定下来
```

E 组是**重新训练**条件下的修复，**不能**解释成"在 A 类 checkpoint 上改两个开关就会自动恢复到预训练匹配水平"。接续已有权重时，要先测 LTE 当前的匹配质量；必要时恢复一份经过核验的干净 LTE 再做适配训练（但恢复 LTE 也会改变参考 value 特征，接收旧特征的主干未必完全不受影响）。

### 4.3 方法论教训

**推理消融和训练消融回答的是不同问题，不能互相推广。** v81 的推理消融说"这份 checkpoint 对 `RefIllumTransfer` 的依赖 ≈ 0"，而训练消融说"训练时保留它能好 +0.9~1.2 dB"。两者都成立——差别在于那份 checkpoint 的该模块**根本没被训练**（lr 回退陷阱）。**推理消融不能被推广成结构结论。**

**配对设计比多跑种子更有效。** 同种子的 A/B 比较能抵消大部分种子噪声（0.8 dB → 0.1 dB 量级）；不同种子的比较噪声大到能淹没待测效应。

---

## 5. 复现方式

### 5.1 训练（LOL 从零，约 11 分钟/组）

```bash
python main.py --dataset LOL --dataset_dir /root/data/datasets/LOLdataset \
  --enhance_mode True --num_res_blocks 8+8+4+2 --n_feats 64 --res_scale 1.0 \
  --freeze_stages= --batch_size 8 --train_crop_size 128 --num_workers 8 \
  --num_init_epochs 2 --num_epochs 28 --print_every 50 --save_every 10 --val_every 10 \
  --lr_rate 1e-4 --lr_rate_lte 1e-5 \
  --rec_w 1.0 --per_w 0.1 --tpl_w 0.1 --tpl_use_S True \
  --illum_smooth_w 1.0 --color_w 0.5 --exposure_w 1.0 \
  --ref_degrade True --load_pretrain False --freeze_lte False --num_gpu 1 \
  --eval_lol_nanobanana True --seed 42 \
  --save_dir <dir> --reset True
```

变体：`--tpl_w 0.0`（去掉 tpl）、`--no_ref_illum True`、`--no_reference True`、`--freeze_lte True`、`--seed 43`。

### 5.2 评测注意

- 要看**干净参考**必须加 `--eval_ref_degrade False`，否则 test loader 继承 `ref_degrade=True`。
- 训练中 eval 只在 `epoch % val_every == 0` 触发，且 `save_every` 不整除 10 的 epoch 不会存盘（本次最大 epoch 28 未保存，用的是 `model_00020.pt`）。

---

## 6. 代码改动清单

```
M option.py                 --seed / --no_reference / --oracle_matching / --no_ref_illum / --ref_illum_const_ref
M main.py                   按 seed 固定随机性
M model/MainNetEnhance.py   forward 新增 use_reference
M model/TTSREnhance.py      无参考短路、oracle_matching 透传、no_ref_illum 解析
M model/SearchTransfer.py   oracle_matching 诊断分支
M model/SearchTransfer.py   覆盖掩码缓存（1 通道 + 缓存，显存 126MB→189KB）
```

全部**默认关闭**，并逐一做过逐位回归验证，不影响任何现有实验行为。

---

## 7. 未决问题

1. **在强模型 + 生成参考 + 跨数据集条件下，修复后的参考分支还能提供多少稳定收益？**
   这是当前最该回答的问题。本报告的所有训练消融都在"从零训练 30 epoch / LOL / HR 或 Nano 参考"下完成；需要一组固定源数据集权重、在目标数据集评测的对照，主结果以 Nano 参考为准，HR 参考仅作诊断。
   建议的配对实验（同一初始 checkpoint、同种子、其余配置一致）：

   | 组 | `RefIllumTransfer` 的 lr | 该模块收到的参考内容 | 回答的问题 |
   |---|---|---|---|
   | F0 | 原微调 lr | 真实参考 | 强模型对照 |
   | F1 | 显式提高一档 | 真实参考 | 修 lr 有没有净收益？ |
   | F2 | 同 F1 | 常量 | F1 的收益是否来自参考内容？ |

2. **`freeze_lte=True` + `tpl_w=0.1` 这一格缺失**（见 4.2），无法完整区分 `tpl` 的直接影响与它通过训练 LTE 产生的间接影响。
3. **亮区匹配更差的机制**（3.1 节）：正确率从暗区 75.4% 单调降到亮区 34.5%，但与光照比值（+0.021）、过曝率（−0.008）都不相关。建议做**保持场景不变的反事实实验**：从同一张 high 构造多种可控低光版本，分别改曝光/局部照明/噪声，其余不变。
4. **门控 S 的正确用法**：`index > full` 三模型一致，但没有找到更好的替代映射。候选：可学习温度、top-k 候选的熵、双向一致性。
5. **`exposure_w` 是否应当下调**：真值 GT 自己就违反该先验（0.217），却与 `rec_w` 同权重。
6. **第 3 节大部分结论建立在 30 epoch 从零训练（~17.5 dB）上**，与微调收敛点（~19.2 dB）的效应量可能不同。所有训练消融只用了 1~2 个种子。
