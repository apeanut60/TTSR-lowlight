# Retinexformer V1 —— 改动、验证与结论

> 日期：2026-09-23/24
> 范围：新增 Retinexformer 主干 + H/4 单点纹理适配器，R0/R1/R2/R3 四组配对，
> 以及后续的配方修复、子集、适配器、解冻 decoder 等六轮验证，
> 最后是 V2「输出端局部参考细化」的两臂 × 3 种子筛选（§17）
> 前置文档：`FINDINGS_reference_branch.md`（参考分支的逐组件诊断）
> 所有数字均为本仓库实测

---

## 0. 怎么读这份文档

**按时间顺序写的，后面的节会修正前面的。** 全篇是"发现 → 撤回过满结论 → 再测"的过程，
不是一份已经定稿的报告。查阅时请优先看后面的节：

| 节 | 内容 | 是否仍然有效 |
|---|---|---|
| §3 / §5 | R0–R3 四组结果与当时的结论 | ⚠️ **大部分作废**，见 §4、§10 |
| §4 | 为什么那四组结果不可用（缺 `--decay`、平坦区） | ✅ 有效 |
| §6 | 当时的下一步建议 | ⚠️ 部分已被后续轮次取代 |
| §9 | 可训练性检查 + 损失逐项归因（exposure 是主因） | ✅ 有效 |
| §10 | 修正配方（`exposure_w=0` + `decay 20`）的效果 | ✅ 有效 |
| §11 | N1：参考光照只在 oracle 参考下有效 | ✅ 有效 |
| §12 | 换 Nano 训练参考无效 | ✅ 有效 |
| §13 | GPU 利用率低的根因（OpenCV 线程池） | ✅ 有效，已修 |
| §14 | A3-only 适配器训练：负结果 | ✅ 有效（§14.4 数字已修正） |
| §15 | 解冻 decoder 尾部 + 纹理配对：仍为负结果 | ✅ 有效 |
| §16 | 当时的"状态与建议"（先修晚期回退） | ⚠️ **已被 §17 取代**，见 §16 开头的说明 |
| §17 | **V2 输出端局部细化：首次正收益（+0.127 / +0.147）** | ✅ 有效，3 种子复现 |
| §18 | V2.1 计划的前置实测核对（代码审查 + 真实数据） | ✅ 有效；修正了 §17.4 的算式笔误 |
| §19 | 按 §18 核对结果修复四处 bug（已测试，未重训） | ✅ 代码已修；**旧 Y0 缓存因此失效** |
| §20 | V2.1 重评：修复后信号仍在，但幅度变小（+0.097 / +0.106） | ✅ 有效，单种子新协议 |

## 0.1 摘要（截至 §17）

**工程侧**

| 结论 | 状态 |
|---|---|
| 新主干与官方 Retinexformer 数值一致 | ✅ wrapper 数据流逐位一致（0.000e+00） |
| 旧 TTSR 路径回归 | ✅ 逐位不变 |
| 训练配方缺 `--decay` | ✅ 已定位并修复 |
| `exposure_w` 与重建目标冲突（占 46% 重建代价中的 26%） | ✅ 已定位，建议置 0 |
| 数据管线被 OpenCV 线程池拖慢 15.6 倍 | ✅ 已修复（epoch 137~180s → 68s） |
| tiled 推理忽略 `--no_ref_illum` / `--no_global_illum` | ✅ 已修复 |
| 评测入口不一致（训练中 vs 独立） | ✅ 已定位（`ref_dir` 静默回退 HR）并统一 |

**科学侧**

| 结论 | 证据 |
|---|---|
| 参考分支的收益完全取决于参考质量 | HR +0.76 vs 生成参考 −0.05（§11） |
| 换训练参考为生成参考**不能**救活它 | Δadapt = −0.005（§12） |
| 只训 H/4 适配器：负结果 | E2−E0 = −0.031，E2−E3 = −0.051（§14） |
| 解冻 decoder 尾部后仍然负 | D1−D0 = −0.035，D1_correct < D1_mismatch（§15） |
| 当前形态已被两轮独立实验否定 | §14 + §15 方向一致 |
| **换成输出端局部细化后首次为正，且 3 种子复现** | Δscheme = +0.127（sd 0.007），Δexternal = +0.147（sd 0.010）（§17） |
| 增益来自**参考内容**，不是"多加了个 CNN" | 同容量 Self 对照 3/3 为负；correct−mismatch = +0.56（§17.2/§17.4） |

**仍未解决（当前最大的不确定性来源）**

1. **评测集不独立**（目前最大）。V2 的正收益已在 3 个种子上复现，但三次用的都是同一批 50 张开发图，
   这些图从头参与了迭代。种子复核只排除了"初始化/数据顺序"的偶然，**没有**排除评测集偶然。
   → 在换到独立数据之前，§17 只能写成"多种子可复现的阳性候选"。
2. **尾部风险**。V2 逐种子最差一张 −1.55 / −2.06 / −1.69 dB，50 张里 9 张在不同种子间正负翻转。
   均值稳定 ≠ 每张都赢。见 §17.5。
3. **晚期回退**（仍然存在，但适用范围要收窄）：几乎每一个 40-epoch 的全量/子集训练都在 ep30 见顶后 ep40 回退
   （含完全无参考的 N0_500）。它是那些实验共有的现象，也是它们各自带 ±0.5 dB 不确定性的原因。
   **注意 §17 不适用**：V2 底座冻结、只训 78k 小模块、按 step 而非 epoch，其轨迹是单调上升的。
   见 §4.2、§10.5、§11.6、§12.6。

---

## 1. 本轮做了什么

### 1.1 新增/修改的代码

| 文件 | 内容 |
|---|---|
| `model/retinexformer_arch.py` **(新)** | 官方实现字节级复制（371 行），加来源/许可证头，去掉 `pdb` import。依赖 torch + einops |
| `model/RetinexRefMainNet.py` **(新)** | `RefTextureAdapter` + `RetinexDenoiser`（在 bottleneck 后插一处）+ `RetinexRefMainNet`（MainNet 契约封装） |
| `option.py` | 新增 `--enhance_backbone {ttsr,retinexformer}`、`--retinex_n_feat`、`--retinex_num_blocks`、`--no_ref_texture`、`--no_global_illum`、`--texture_lv3_only` |
| `model/TTSREnhance.py` | 主干选择；pad 到 4 的倍数并裁回；`use_texture` / `apply_ref_illum` / `apply_illum` 三档独立开关；`inject_lv2/lv1` 透传 |
| `model/MainNetEnhance.py` | `forward` 新增 `inject_lv2` / `inject_lv1`（供 R3 对照） |
| `trainer.py` | `freeze_stages` 空匹配断言；`ref_illum` 组非空断言；参数重复检测；逐组 LR 日志；`save()` 抽出 |
| `main.py` | 训练结束强制保存最后完成的 epoch；`--eval` 也注入生成参考评测 loader |
| `run_retinex_v1.sh` **(新)** | 四组配对运行脚本 |

**环境**：需要 `einops`（原环境没有，已装 0.8.2）。

### 1.2 融合设计

```
low[-1,1] → [0,1] → Illumination_Estimator → Xc → encoder → bottleneck(dim·2^level, H/4)
                                                                    │
low/ref → 冻结 LTE → SearchTransfer → S, T_lv3(256ch, H/4) → A3 ─────┤
                                                                    ▼
                                            decoder → Y01 → [-1,1] → RefIllumTransfer(整图一次) → sr
```

```
A3 = Conv1x1(dim+256 → dim) → GELU → Conv3x3(dim → dim, pad=1)   # 末层零初始化
F3_new = F3 + sigmoid(S) · A3(cat(F3, T_lv3))
```

默认 `n_feat=40`、`num_blocks=[1,2,2]`（与官方 LOL-v1 配置一致），瓶颈 160 通道，故 A3 输入 416。

### 1.3 训练配置

| 项目 | 值 |
|---|---|
| 数据 | **data1 (LSRW)**，5600 训练对（Huawei 2450 + Nikon 3150），50 张评测（HW 30 + NK 20） |
| batch / crop / seed | 8 / 128 / 42 |
| 阶段 | 2 init + 40 常规 epoch |
| LR | `lr_rate 1e-4`、`lr_rate_refillum 1e-4` 显式、LTE 冻结排除 |
| **LR 调度** | **无**（脚本漏传 `--decay`，默认 999999）← 见 4.1 |
| 损失 | rec 1.0 / per 0.1 / illum_smooth 1.0 / color 0.5 / exposure 1.0；tpl=adv=ref_correct=illum_match=0 |
| 关闭 | RefCorrection、GlobalIllumHead |
| 评测 | `data1`（HR 参考，诊断）+ `data1_nanobanana_huawei` / `_nikon`（生成参考，主结果） |
| 耗时 | 约 107 分钟/组，四组约 7 小时 |

---

## 2. 验收清单（不训练的验证）

| 验收项 | 结果 |
|---|---|
| 旧默认 TTSR 路径回归 | ✅ 五个输出**逐位不变**（0.000e+00） |
| 与官方 backbone 数值一致 | ✅ 同 `[0,1]` 输入下 internals **逐位一致**（0.000e+00） |
| — 经 `[-1,1]↔[0,1]` 往返则差 5.376e-05 | 网络对该量级扰动放大 **~420×**（往返误差 1.49e-08）。计划的 `<1e-6` 阈值在此口径下不可达，应改为同标度比较（→0）或放宽到 ~1e-4 |
| 适配器零初始化时开纹理无影响 | ✅ 0.000e+00 |
| 末层零初始化不永久阻断前层 | ✅ 第 0 步 `conv2.grad=5.34e-02`、`conv1.grad=0.000e+00`；**一步后 `conv1.grad=2.21e-02`** |
| `no_reference` 换参考输出不变 | ✅ 0.000e+00（Retinex 估计器仍执行，因为它只用 low） |
| `no_ref_texture` 时 RefIllumTransfer 仍收参考 | ✅ 换参考输出差 7.47e-03 > 0 |
| 非 4 倍数尺寸 | ✅ 255×257 padding 到 256×260，输出裁回 255×257，S/T 网格 63×64 同步裁齐 |
| 网格不匹配时报错 | ✅ 抛 ValueError 并提示需 pad 到 4 的倍数 |
| `freeze_stages` 用错主干报错 | ✅ 新主干 + 旧 stage 名 → RuntimeError；空串 → 正常 no-op |
| 参数组与 LR 记录 | ✅ 初始化时逐组打印 + 每 epoch 打印全部组 |
| 最终 checkpoint 强制落盘 | ✅ 实测 `num_epochs=3, save_every=10` 会保存 `model_00003.pt` |

**实测 optimizer 分组**（R0/R1/R2）：`g1 = 126 tensors`（主干+适配器，1e-4）、`g2 = 6 tensors`（ref_illum，1e-4）。R3 为 4 组。

**未实现**：训练恢复（optimizer / scheduler / global_step / RNG）。`trainer.load()` 只做部分权重加载且仅用于 `--test/--eval`。计划里"能恢复继续训练"是**新需求**，本轮未做。

---

## 3. 四组配对实验的结果

### 3.1 设计

| run | 主干 | 纹理 | 参考光照 | 用途 |
|---|---|---|---|---|
| R0 | retinexformer | 关 | 关 | 无参考基线 |
| R1 | 同 R0 | 关 | 开 | 参考光照增量 |
| R2 | 同 R0 | T_lv3 单点 | 开 | 完整 V1 |
| R3 | **旧 ttsr 主干** | 只注入 T_lv3 | 开 | 主干对照（计划外，我加的） |

R3 的作用：计划把"换主干"和"纹理从三点改单点"绑在一起，R2−R3 才能隔离主干。

### 3.2 结果（PSNR）

**epoch 30**

| 参考设定 | R0 | R1 | R2 | R3 |
|---|---|---|---|---|
| HR 参考（50 张，诊断） | 18.144 | 18.783 | 18.857 | 18.478 |
| Nano-Huawei（30 张） | 19.057 | 19.335 | 19.375 | 18.805 |
| Nano-Nikon（20 张） | 16.768 | 16.689 | 16.631 | 16.562 |
| **Nano 加权（主结果）** | **18.141** | **18.277** | **18.277** | **17.908** |

**epoch 40**

| 参考设定 | R0 | R1 | R2 | R3 |
|---|---|---|---|---|
| HR 参考 | 18.258 | 18.818 | 18.881 | 17.903 |
| Nano-Huawei | 19.304 | 19.482 | 19.393 | 18.430 |
| Nano-Nikon | 16.682 | 16.598 | 16.811 | 16.484 |
| **Nano 加权（主结果）** | **18.255** | **18.328** | **18.360** | **17.652** |

**增量**

| 对比 | ep30 (HR / Nano加权) | ep40 (HR / Nano加权) |
|---|---|---|
| R1 − R0（参考光照） | +0.639 / +0.135 | +0.560 / +0.073 |
| R2 − R1（纹理） | +0.074 / **+0.001** | +0.063 / +0.032 |
| R2 − R0（完整参考） | +0.713 / +0.136 | +0.623 / +0.105 |
| R2 − R3（主干） | +0.379 / +0.370 | +0.978 / +0.709 |

### 3.3 收敛性（HR 参考，ep10/20/30/40）

| run | 序列 | 最好 |
|---|---|---|
| R0 | 17.734 18.230 18.144 18.258 | ep40 |
| R1 | 18.723 **18.969** 18.783 18.818 | **ep20** |
| R2 | 18.420 18.827 18.857 18.881 | ep40 |
| R3 | 17.902 18.313 **18.478** 17.903 | **ep30** |

**四组里两组非单调**，R3 在最后 10 个 epoch 掉 0.575 dB。

---

## 4. 诊断：为什么这些结果不可用

### 4.1 根因一：脚本漏了 `--decay`（我的 bug）

从日志逐 epoch 打印的 LR 可以确认：

```
R0/R1/R2/R3: 1.000e-04 × 42 个 epoch      ← 一次都没降
```

`option.py` 的 `--decay` 默认值是 **999999**，而 `run_retinex_v1.sh` 没有传。仓库历史约定：

```
--decay 10 : 54 个脚本      --decay 20 : 39 个脚本（最近的 p10-rgmsa 用它）
--decay 30 :  7 个脚本      --decay 999999 / 不传 : 28+14 个
```

**已修**：脚本现在传 `--decay 20 --gamma 0.5`。

### 4.2 根因二：模型从 init 结束就停在平坦区（**未修**）

每 epoch 平均 `rec_loss`：

```
R2:  init ep1/2 = 0.204 / 0.187      ← 全程最好的重建损失在 init 结束时
     常规 epoch = 0.223 0.224 0.219 ... 0.200 0.210    (40 个 epoch 只降 0.018)
R3:  init ep1/2 = 0.266 / 0.220
     常规 epoch = 0.247 0.249 0.227 ... 0.246 0.255
```

**最好的重建损失出现在 init 阶段结束时，之后 40 个 epoch 反而停在更差的位置。** 常规 epoch 一启用辅助损失（`exposure_w 1.0` / `illum_smooth_w 1.0` / `color_w 0.5`，与 `rec_w 1.0` **同权重**），重建立刻变差并再也不恢复。

在这个平坦区里，各 epoch 的评测值相差 ±0.6 dB，**而这个差异不由任何被记录的目标函数解释**。

这条与前置文档的一条未决问题吻合：**真值 GT 自己就违反 exposure 先验**——GT 上的 exposure loss = 0.217，比模型输出的 0.15 还高，却与 rec 同权重。

### 4.3 从 R3 的 ep30 续训 10 epoch（决定性对照）

| | 起点 ep30 | lr 1e-4 @+5 | lr 1e-4 @+10 | lr 1e-5 @+5 | lr 1e-5 @+10 |
|---|---|---|---|---|---|
| HR 参考 | 18.478 | 18.271 | **17.647** | 18.092 | **18.038** |
| Nano-HW | 18.805 | 18.824 | **18.106** | 18.695 | **18.568** |
| Nano-NK | 16.562 | 16.356 | **16.027** | 16.415 | **16.288** |

- **1e-4 复现塌陷**（17.647，与原始 run 的 ep40 = 17.903 同向同量级）
- **1e-5 只把损伤减半**，**没有止住**
- 两个 LR 下三个评测集全部单调下降

→ **降 LR 不是根治手段。**

### 4.4 已排除的假设

| 假设 | 检验 | 结论 |
|---|---|---|
| 权重发散 / 梯度爆炸 | checkpoint 范数：R2 71.4→74.5、R3 55.8→61.1；max\|w\| 恒为 4.464 | ❌ 排除，平滑增长 |
| 训练用退化参考 / 评测用干净参考的不匹配 | 用 `eval_ref_degrade=True`（与训练一致）重评 R3 四个 ckpt：17.951 / 18.252 / 18.452 / 17.971 | ❌ 排除，曲线与干净参考几乎重合（都峰值 ep30、都掉到 ~17.9） |
| 辅助损失把模型带偏 | 逐 epoch 均值（前10 vs 后10）：rec −0.018、per −0.003、exposure **+0.011**、color −0.0006、smooth −0.0003 | ❌ 不成立，没有显著的方向性 |
| 过拟合 | 训练损失全程不降（见 4.2） | ❌ 不像经典过拟合 |

---

## 5. 能站住 / 必须撤回的结论

### 5.1 能站住

1. **R2 > R3 的符号**：ep30 与 ep40 × 三个参考设定，**6/6 为正**。但幅度不可信（见下）。
2. **在 data1 + 生成参考下，整个参考分支的增量很小**：Nano 加权 +0.136（ep30）/ +0.105（ep40）。两个 epoch 一致。
3. **参考质量的影响远大于参考机制**：同样的四组，HR 参考下的增量是 +0.62~+0.71，生成参考下只有 +0.10~+0.14，差 5~6 倍。
4. **工程正确性**（第 2 节全部验收项）。

### 5.2 必须撤回 / 降级

1. **R2 − R3 的幅度**（我上一轮报的 +0.452 / +0.709）。它被 R3 的 ep40 回退放大。保守值是 ep30 的 **+0.37**，但仍有参数量混淆：R3 是旧主干（`8+8+4+2` ResBlock、n_feats=64），参数量远大于 Retinexformer（n_feat=40）。**R2−R3 同时包含"换主干"和"模型变小/更不易训崩"。**
2. **所有以单个 epoch 绝对值做的比较**。四组里两组非单调。
3. **"参考分支 +0.1 dB"不能作为结论**——它低于前置文档实测的种子噪声（0.2~0.8 dB），单种子测不出来。

### 5.3 需要补 caveat 的旧结论

前置文档（`FINDINGS_reference_branch.md`）里的 LOL 消融**同样没有 `--decay`**，也是在这套平坦区里测的：

- **仍然成立**：`tpl` 破坏匹配器（45.1% → 6.9%）——机制性效应，不依赖 PSNR 噪声；`RefIllumTransfer` 的训练价值 +1.06~1.64 dB——远大于种子噪声。
- **需要加 caveat**：`tpl` 的 −0.1 dB、修复组合的 +0.31 dB——**方向可信，效应量可疑**。

---

## 6. 下一步（按性价比）

### A（推荐，最便宜）先做辅助损失消融

把 `exposure_w / illum_smooth_w / color_w` 从 1.0 / 1.0 / 0.5 降到 0.1（或 0），跑 20 个 epoch，看两件事：

- `rec_loss` 是否能在 init 之后**继续下降**（而不是停在 0.20+）
- 评测曲线是否变**单调**

成本：约 46 分钟。**这是唯一能直接验证 4.2 节假设的实验。**

### B 确认 ep30→ep40 的塌陷是否系统性

用 `SEED=43` 重跑 R3 到 40 epoch（约 107 分钟），看是否同样峰值在 ep30。若不同 → 种子噪声主导，加种子无用，必须先修训练。

### C 直接加 `--decay 20` 重跑四组

```bash
bash run_retinex_v1.sh              # 四组约 6.7 h
SKIP_R3=1 bash run_retinex_v1.sh    # 三组约 5.0 h
```

**不建议先做**：4.3 节已证明降 LR 10 倍只把损伤减半，decay 治不了根。

### D 参数量对齐（若要做干净的主干比较）

R3 应把 `n_feats` 降到与 Retinexformer 可比，或反过来加宽 Retinexformer。否则 R2−R3 永远说不清。

---

## 7. 复现方式

### 7.1 训练（data1）

```bash
tmux new -s retinex
cd /root/projects/TTSR-lowlight
EPOCHS=40 bash run_retinex_v1.sh 2>&1 | tee /root/data/experiments/retinex_v1_driver.log
```

旋钮：`EPOCHS`(40) `SEED`(42) `SKIP_R3`(0) `VAL_EVERY`(10) `PRINT_EVERY`(100) `OUT_ROOT` `DATA`

### 7.2 单独评测一个 checkpoint（`--eval` 现在也会注入生成参考 loader）

```bash
python main.py --dataset data1 --dataset_dir /root/data/datasets/data1 \
  --enhance_mode True --enhance_backbone retinexformer \
  --retinex_n_feat 40 --retinex_num_blocks 1,2,2 --freeze_stages= \
  --num_workers 8 --num_gpu 1 --ref_correction False --no_global_illum True --tpl_w 0.0 \
  --eval True --model_path <ckpt> --eval_ref_degrade False --eval_data1 True \
  --save_dir <原实验目录>
```

会一次打出 `data1`（HR 参考）+ `data1_nanobanana_huawei` + `data1_nanobanana_nikon`。

### 7.3 从 checkpoint 续训（本轮用的技巧，不需要改代码）

```bash
python main.py ... --load_pretrain True --pretrain_path <ckpt> \
  --num_init_epochs 0 --num_epochs 10 --lr_rate <LR> --decay 999999 ...
```

`TTSREnhance.load_pretrained_weights` 会加载所有形状匹配的键（实测 `Loaded 204/204`）。

### 7.4 本轮实验产物

```
四组主实验 : /root/data/experiments/retinex_v1_data1_s42/{R0_noref,R1_refillum,R2_refillum_tex3,R3_ttsr_tex3only}/
续训对照   : /root/data/experiments/r3_continue_{lr1e-4,lr1e-5}/
退化参考评测: /root/data/experiments/r3_deg_ep{10,20,30,40}/
驱动日志   : /root/data/experiments/retinex_v1_data1_s42_driver.log
```

---

## 8. 关键数字速查

```
data1 规模          : 5600 训练对 / 50 评测图
单 epoch 耗时       : 137 s (700 batch @ batch8)，瓶颈是磁盘 I/O
并行无用            : 两组并行各自慢 2 倍，聚合吞吐不变
单组总耗时          : 约 107 min (2 init + 40 epoch + 4 次评测)
每次评测            : 约 70 s (100 张，tiled tile=256)

数据流等价性        : 同 [0,1] 输入下 wrapper vs 官方 = 0.000e+00
适配器零初始化      : 开/关纹理输出差 = 0.000e+00
conv1 梯度          : 第 0 步 0.000e+00 → 一步后 2.21e-02

LR 轨迹             : 1.000e-04 × 42 (未衰减)
R3 塌陷             : 18.478 (ep30) → 17.903 (ep40) = -0.575 dB
续训 1e-4 (10ep)    : 18.478 → 17.647
续训 1e-5 (10ep)    : 18.478 → 18.038
退化参考评测        : 17.951/18.252/18.452/17.971  (与干净参考几乎重合)
权重范数            : R2 71.4→74.5, R3 55.8→61.1, max|w|=4.464 恒定
init 结束的 rec_loss: R2 0.187, R3 0.220  ← 全程最低点
```

---

## 9. 补充：可训练性检查与损失归因（对应 V1.1 计划 §5）

> 脚本：`scripts/trainability_check.py`（新增）
> 协议：data1 训练集前 8 个样本的固定 128×128 中心裁剪，无增强、无 shuffle，
> lr 固定 1e-4，各 arm 从**同一初始权重**出发跑 1000 步，
> 记录指标统一为 `L1(sr, hr)`（与各 arm 实际优化的目标无关，保证可比）。

### 9.1 §5.1 可训练性检查：通过

| arm | 首窗 L1 | 末窗 L1 | 降幅 | 判定门槛 ≥50% |
|---|---|---|---|---|
| rec-only | 0.2737 | **0.0375** | **86.3%** | PASS |
| old-recipe（全套） | 0.2902 | **0.0567** | **80.5%** | PASS |

**模型完全可训练**：8 个固定 crop 上 1000 步能把 L1 降到 12.6%。所以第 4.2 节观察到的"真实训练损失平坦"**不是**数据通路、梯度或优化器的根本缺陷。

### 9.2 §5.2 逐项归因：`exposure` 是主因

按计划的判定树（L0 正常 / L2 变差 → 分别只加 TV、exposure、color）逐项测：

| arm | 首窗 L1 | 末窗 L1 | 相对 rec-only |
|---|---|---|---|
| rec | 0.25368 | 0.04041 | — |
| color (0.5) | 0.25753 | 0.04095 | **+1.3%** |
| smooth (1.0) | 0.25398 | 0.04179 | **+3.4%** |
| per (0.1) | 0.25186 | 0.04340 | **+7.4%** |
| **exposure (1.0)** | 0.26909 | 0.05084 | **+25.8%** |
| old（全部四项） | 0.26514 | 0.05896 | **+45.9%** |

**`exposure` 一项占了 45.9 个点里的 25.8 个。** 且差距随训练扩大（200 步时全套差 24%，1000 步时 45.9%）——符合"模型持续用重建换辅助目标"的形态。

### 9.3 机制：不是目标值错，是损失形式与 GT 冲突

先排除"目标值设错"。data1 训练集 GT 的 16×16 patch 均值分布（120 张 128×128 crop，7680 个 patch）：

```
均值 0.3990   中位 0.3876   std 0.1968
分位: 5%=0.1143  25%=0.2410  75%=0.5223  95%=0.7582

ExposureControlLoss(target) 对 GT 的取值:
  target=0.400 -> 0.15994    ← 当前配置
  target=0.388 -> 0.15963    ← 理论最优（中位数）
```

**`exposure_target=0.4` 已接近最优（差 0.2%）**，所以调 target 没用。

真正的问题是**损失形式**：`ExposureControlLoss` 惩罚**每一个 16×16 patch** 与其常量目标的偏离，而 GT 的 patch 均值从 0.114 铺到 0.758（**std 0.197**）。任何常量目标都会留下 ~0.16 的残差——那不是模型没学好，那是**真值本身的局部亮度方差**。

因此该损失在强制模型**压平局部色调**，而它的权重 `exposure_w=1.0` **与 `rec_w` 同权重**。

**根本原因**：Zero-DCE 的曝光损失是为**无监督**训练设计的（没有 GT，需要先验约束亮度）。**本项目有配对 GT，重建项已经监督了亮度**——该先验是冗余的，且实测有害。

### 9.4 配方建议

| 项 | 重建代价 | 建议 |
|---|---|---|
| `exposure_w 1.0` | **+25.8%** | **设为 0**（配对训练下冗余且有害） |
| `per_w 0.1` | +7.4% | 保留（感知质量换 PSNR 的正常代价） |
| `illum_smooth_w 1.0` | +3.4% | 保留或降到 0.1 |
| `color_w 0.5` | +1.3% | 保留 |

同时向 V1.1 计划补一条它未覆盖的原则：**本项目有配对 GT，所有从无监督方法继承来的先验损失（exposure / color / 部分 TV）都应单独验证是否还有存在必要，而不是沿用权重。**

### 9.4b 直接的配方验证：`rec` / `old` / `old 去掉 exposure`

三个 arm 从同一初始权重跑 1000 步，读 **step 1000** 的 `L1(sr,hr)`：

| arm | step 1 | **step 1000** | 相对 rec-only |
|---|---|---|---|
| rec | 0.62677 | **0.03660** | — |
| old（全套四项，exposure 1.0） | 0.62677 | **0.05353** | **+46.3%** |
| **fixed（去掉 exposure）** | 0.62677 | **0.04150** | **+13.4%** |

**只去掉 `exposure_w` 一项，重建代价从 +46.3% 降到 +13.4%——消掉了 71% 的惩罚。** 与 9.2 节的单项归因一致（exposure 单独 +25.8%，其余三项合计 ≈ +13.4%）。

附带观察：`old` 的越界比例最低（step 1000 时 0.051% vs rec 的 0.321%），说明曝光先验确实有抑制输出饱和的副作用。但已训练的四个 checkpoint 在真实评测图上越界率都是 **0.0000%**，所以这个副作用在本项目里没有实际价值。

### 9.5 对第 4.2 节的回填

第 4.2 节观察到"init（只有 rec）的 rec_loss 0.187 比常规 epoch（全套）的 0.20~0.23 低约 19%"。本节在固定 crop 上独立测得全套损失的重建代价是 **+45.9%**（其中 exposure 占 25.8）。两者方向一致、量级不同（真实训练无法像 8 个 crop 那样充分过拟合），说明 4.2 节的平坦区至少**部分**由该损失冲突造成。

**仍未解释**：评测曲线在 ep20/ep30 见顶后回退（±0.6 dB 的非单调性）。损失归因只能解释"重建目标被抵消"，不能解释"为什么某个 epoch 恰好更好"。这部分仍待 §6 的正式对照回答。

---

## 10. 修正配方的验证：N0（`exposure_w 0` + `decay 20`）

> 目的：无论后续怎么走，N0（无参考基线）都是 N0/N1/N2 对比的必需臂，所以先跑它；
> 同时它与 R0 只差两处（`exposure_w 1.0→0`、`decay 999999→20`），可直接看配方修复的效果。
> 配置：data1，retinexformer n_feat=40，`no_reference=True`，2 init + 40 epoch，seed 42。

### 10.1 曲线（HR 参考，50 张）

| run | ep10 | ep20 | ep30 | ep40 |
|---|---|---|---|---|
| **N0（修正配方）** | 18.614 | 18.703 | 18.870 | **18.926** |
| R0（旧配方） | 17.734 | 18.230 | 18.144 | 18.258 |
| 差值 | **+0.880** | +0.473 | +0.726 | **+0.668** |

**N0 在 HR 上完全单调**（18.614 → 18.703 → 18.870 → 18.926），R0 在 ep30 出现回退。

### 10.2 归因：增益主要来自 `exposure_w`

`--decay 20` 在第 20 个常规 epoch 才生效（实测 LR 轨迹：第 20 格从 1.000e-04 变 5.000e-05）。
所以 **ep10 与 ep20 的差值（+0.880 / +0.473）是纯粹的 exposure 效应**，ep30/ep40（+0.726 / +0.668）才含 decay。增益不随 decay 生效而变大 → **主要来自去掉 exposure**。

顺带确认 V1.1 计划指出的 StepLR off-by-one：计划期望"第 21 轮开始 5e-5"，实测是**第 20 轮**就用 5e-5（`scheduler.step()` 在 epoch 开头调用）。

### 10.3 三个参考设定的完整对照

| 设定 | ep10 | ep20 | ep30 | ep40 |
|---|---|---|---|---|
| HR（诊断） | +0.880 | +0.473 | +0.726 | +0.668 |
| Nano-Huawei | +1.232 | +0.947 | +1.261 | +0.916 |
| Nano-Nikon | +0.351 | **−0.237** | −0.076 | +0.298 |
| **Nano 加权（主）** | **+0.880** | +0.473 | +0.726 | **+0.669** |

12 个比较里 **10 个为正**，均值约 **+0.62 dB**（唯一为负的是 Nikon 的 ep20/ep30）。

### 10.4 最重要的含义：参考分支的旧结论全部作废

把 N0（**完全无参考**，修正配方）与四组旧配方模型对比（epoch 40）：

| 设定 | N0（无参考，修正配方） | R2（参考光照+纹理，旧配方） | R1 | R3 | R0 |
|---|---|---|---|---|---|
| HR | **18.926** | 18.881 | 18.818 | 17.903 | 18.258 |
| Nano 加权 | **18.924** | 18.360 | 18.328 | 17.652 | 18.255 |

**一个完全没有参考分支的模型，在修正配方下超过了所有带参考的旧配方模型**（HR +0.045、Nano +0.564）。

这说明：**之前"参考有增益/无增益"的全部比较都是被训练配方污染的。** 参考分支在 data1 上的价值必须用修正配方重新测（即计划的 N0/N1/N2），旧结论（第 3、5 节里那些 +0.1 ~ +0.7 的增量）不能再引用。

### 10.5 仍未解决的问题

1. **非单调性没有完全消除**：N0 在 Huawei 上 ep40 回退（20.318 → 20.220）、在 Nikon 上 ep20 回退（16.940 → 16.541）。HR 上单调，所以问题减轻但未根除。
2. **仍是单种子**。+0.62 的均值大于 0.2~0.8 的种子噪声，且 10/12 一致为正，但严格结论仍需 seed 43。
3. **decay 的独立贡献未知**——因为 ep10/ep20 时 decay 还没生效，无法从本轮分离。若要分离需再跑一组"exposure 0 + 无 decay"。

---

## 11. N1：参考光照在修正配方下的验证

> 依据 V1.1 后续计划（`RETINEX_NEXT_PLAN_N1_AND_CAMERA.md`）§B2/§B3。
> 本轮唯一新增的正式实验；N0 复用第 10 节的结果。

### 11.1 设计

| 项 | 值 |
|---|---|
| N0 | Retinexformer，`no_reference=True`（纹理关 + 参考光照关），修正配方 |
| **N1** | 同主干、同配方，`no_reference=False` + `no_ref_texture=True` + `no_ref_illum=False` |
| 唯一差异 | **`RefIllumTransfer` 是否启用**（纹理两条都关） |
| 共同配置 | data1 混训，batch 8 / crop 128 / seed 42，2 init + 40 epoch，`exposure_w 0`，`decay 20`，`lr_rate_refillum 1e-4` 显式，LTE 冻结 |
| 训练参考 | HR 随机裁剪 + 退化（与 N0 一致） |

**初始化公平性（已验证）**：`no_reference` / `no_ref_texture` 只在 `forward` 里读取，不影响构造顺序。实测两种配置在 `seed 42` 下 **156/156 个张量逐位相同**。N1 的 `model/init.pt` 也已落盘并核验，对 N0/N1 两种配置重建后均逐位一致，可作为后续 arm 的共享初始权重。

**A1 验收（400×600，触发分块的尺寸，用 N0 的 ep40 权重）**：

```
global_illum_head 是否为 None : True
换参考后输出最大差          : 0.000e+00
ref_illum 被调用次数         : 0
输出形状 (1,3,400,600) / 有限值 : True
```

### 11.2 结果：N1 − N0（dB）

| 参考设定 | ep10 | ep20 | ep30 | ep40 |
|---|---|---|---|---|
| **HR 参考**（诊断，50 张） | **+0.884** | **+0.836** | +0.565 | **+0.764** |
| Nano-Huawei（30 张） | +0.169 | −0.215 | −0.275 | −0.048 |
| Nano-Nikon（20 张） | −0.033 | +0.393 | +0.112 | −0.045 |
| **Nano 加权（主结果）** | +0.088 | +0.028 | −0.120 | **−0.047** |

绝对值（ep40）：

| 设定 | N1 | N0 | Δ |
|---|---|---|---|
| HR 参考 | **19.690** | 18.926 | **+0.764** |
| Nano-Huawei | 20.172 | 20.220 | −0.048 |
| Nano-Nikon | 16.935 | 16.980 | −0.045 |
| Nano 加权 | 18.877 | 18.924 | −0.047 |

### 11.3 结论：参考光照只在 oracle 参考下有效

**HR +0.764 dB vs 生成参考 −0.047 dB，相差 16 倍。** 而且 Nano 上不是"无收益"而是**略微有害**（华为 20.220→20.172、尼康 16.980→16.935）。

**跨配方可复现**：

| 配方 | HR 参考增量 | 生成参考增量 |
|---|---|---|
| 旧配方（第 3 节，R1−R0） | +0.560 | +0.073 |
| 旧配方（第 3 节，R2−R0） | +0.623 | +0.105 |
| **修正配方（本节，N1−N0）** | **+0.764** | **−0.047** |

两套配方都显示同一模式：**Oracle 参考下有明显增益，生成参考下 ≈ 0**。所以这不是配方造成的假象，而是一个稳定的现象。

**对第 10.4 节的修正**：第 10 节说"参考分支的旧结论全部作废"——更准确的表述是**旧结论的数值作废、但"参考质量决定收益"这个结论保留并加强**：不是"参考没用"，而是**"生成参考携带的 tone 信息不构成可靠信号"**。

### 11.4 机制假说与下一步

**假说**：训练用 **HR 随机裁剪**当参考，等于在教这个模块"相信参考图的色调"。部署时换成**生成参考**，其色调分布不同且不可靠，模块学到的信任被误用，因此 Nano 上略微拖后腿。

这正好落在计划 §B5 判定表的 **"仅 HR 参考有效，Nano 无效"** 一格，对应的动作是"优先归类为参考来源/训练分布问题；不能凭此要求拆设备模型"。**计划这个判断是对的，现在有了明确证据。**

因此**下一步该换的是训练参考，不是结构**：

```
N1-nano = N1 的结构与配方完全不变，只把训练参考换成生成参考（Nano）
          → 与 N1 对比，看 Nano 评测下 ref_illum 是否变成正收益
```

data1 训练集本身已有 `Training data/{Huawei,Nikon}/nanobanana_ref`，且 `dataset/data1_nanobanana.py` 已支持（`--data1_nanobanana_ref_subdir`），不需要新造数据。

### 11.5 本轮产物

```
N0  : /root/data/experiments/retinex_v11/N0_fixed_s42/   (ep40 18.926 HR / 18.924 Nano)
N1  : /root/data/experiments/retinex_v11/N1_fixed_s42/   (ep40 19.690 HR / 18.877 Nano)
      └ model/init.pt  ← 已核验的共享初始权重（12.1 MB）
日志: /root/n0_fixed.log, /root/n1_fixed.log
```

### 11.6 仍未解决

1. **单种子**。+0.764 / −0.047 都要靠 seed 43 定噪声区间。
2. **N1 的 HR 曲线仍非完全单调**（19.498 → 19.539 → 19.435 → 19.690，ep30 有回退）。N0 在 HR 上单调，所以修正配方减轻但未根除该现象。
3. **纹理那一半（lv3 适配器）从未在修正配方下测过**。若要完整分解参考分支，还需要 N2（N1 + lv3 纹理）。

---

## 12. 匹配子集实验：换训练参考能否救活参考光照分支

> 依据 `N1_NANO_EXECUTION_PLAN.md`。该计划的 Step A1 要求先核实 Nano 参考在**训练集**上的覆盖率，
> 并规定"不完整就不启动全量单变量对照，改走匹配子集"。

### 12.1 A1 覆盖率门槛：不通过

```
训练集 Huawei : low 2450 / high 2450 / nanobanana_ref 269
训练集 Nikon  : low 3150 / high 3150 / nanobanana_ref 250
manifest      : Huawei.txt 250 行 + Nikon.txt 250 行
_collect_triplets 实际可收集 : 500 个
data1.TrainSet 总数          : 5600 对
覆盖率 = 500 / 5600 = 8.9%
```

`_collect_triplets` 要求参考同名存在才收录（**无静默 HR 回退** ✓），代价是**静默把训练集缩到 500**。
按计划规定改走"匹配子集对照"。

### 12.2 匹配子集的实现与验证

新增 `--train_manifest_dir`（`dataset/data1.py` 的 `_restrict_to_manifest`），让 `data1.TrainSet` 也能限定到同一批样本。

```
data1（无 manifest）           : 5600 对   ← 旧行为不变
data1 + manifest              :  500 对
data1_nanobanana              :  500 tripet
两者的 low 样本集合完全相同    : True（交集/并集 = 500/500）
```

另外确认切 dataset 名**不改变评测入口**：`data1_nanobanana` 直接 `from dataset.data1 import TestSet`，是同一个类、同一批 50 张、同样 HR 参考。

### 12.3 三臂设计

| run | 数据集 | 训练参考 | 参考光照 |
|---|---|---|---|
| N0_500 | data1 + manifest | 无 | 关 |
| N1HR_500 | data1 + manifest | HR 随机裁剪 | 开 |
| N1Nano_500 | data1_nanobanana | Nano 参考 | 开 |

共同：`--load_pretrain` 加载 N1 的 `init.pt`（实测 `Loaded 156/156`）、`exposure_w 0`、`decay 20`、seed 42、2 init + 40 epoch。
**三臂训练样本完全相同**（500/500），唯一变量是参考来源 ✓

### 12.4 结果（ep40）

| run | HR（50 张） | Nano-HW（30） | Nano-NK（20） | **Nano 加权（主）** |
|---|---|---|---|---|
| N0_500 | 18.690 | 19.813 | 16.999 | **18.687** |
| N1HR_500 | **19.193** | 19.735 | 16.946 | **18.619** |
| N1Nano_500 | 18.631 | 19.736 | 16.931 | **18.614** |

```
Δadapt  = N1Nano − N1HR = −0.005 dB     ← 换 Nano 训练参考：无效
Δuseful = N1Nano − N0   = −0.073 dB     ← 相对无参考：无净收益
对照    = N1HR   − N0   = −0.068 dB     ← HR 训练参考在 Nano 上也无收益
```

### 12.5 结论：假说被否证

计划 §1 的假说——"用 HR 参考训练的光照模块没有适应生成参考，换成 Nano 训练参考就能救活"——**不成立**。换训练参考后 Nano 评测只变化 −0.005 dB（即零）。

同时 HR 行印证了机制的对称性：

| 训练参考 | HR 评测增量 | Nano 评测增量（Nano 加权） |
|---|---|---|
| HR 裁剪 | **+0.503** | −0.068 |
| Nano | −0.059 | −0.073 |

**这个模块只是"学会了相信训练参考的色调"**：用什么参考训练，就只在那种参考上有效；换成部署用的生成参考后，它既无正收益也无明显负收益。

### 12.6 必须声明的限制

1. **500 样本子集（8.9% 训练数据），模型明显欠训**：全量 N0 的 HR 是 18.926，子集 N0_500 只有 18.690。
2. **单种子**，而 −0.005 / −0.073 都**远低于** 0.2~0.8 dB 的种子噪声 → 严格讲三臂在 Nano 上**不可区分**，不能说"Nano 训练参考有害"，只能说"测不出增益"。
3. **晚期回退仍在**：三臂的 Nano 加权都在 ep30 见顶后 ep40 回退。

按计划 §6 的停止规则，落在 **"Nano 训练后仍没有净收益 → 暂缓该分支调参，保留 N0；不外推为所有参考方法无效"**。

---

## 13. 数据管线性能诊断：GPU 利用率低的根因

> 触发：用户观察训练时 GPU 利用率很低。

### 13.1 分解

| 环节 | 实测 |
|---|---|
| 纯前向（batch 8, crop 128, 数据已在 GPU） | **13.2 ms** |
| 完整训练步（前向+反向+优化器+per 的 VGG） | **73.3 ms** |
| 实测训练（修复前） | **~196 ms/batch** |
| 独立测 DataLoader（修复前, 8 workers） | **~240 ms/batch** |

→ 训练步里约 **63% 在等数据**，GPU 有效占用仅约 37%。

### 13.2 根因：OpenCV 线程池 × cgroup CPU 配额

```
cpu.max        = 1000000 100000        ← 容器只有 10 核配额
nproc 报告     = 128
nr_throttled   = 433498 / 758380       ← 57% 的调度周期被限流
throttled_usec = 1,907,614,903,648
load average   = 29.7 / 44.0 / 46.7    ← 超配额 3~5 倍
cv2.getNumThreads() = 128 ; torch.get_num_threads() = 64
```

OpenCV / PyTorch 按 `nproc` 定线程池 → **每 worker 128 个 OpenCV 线程 × 8 workers = 1024 线程抢 10 个核**。

### 13.3 实测：修掉线程池后的差距

| 配置 | ms/batch |
|---|---|
| 默认（cv2 每 worker 128 线程） | 239.9 |
| worker 内限 1 线程 | **15.4**（快 15.6 倍） |
| worker 内限 1 线程, 4 workers | 32.4 |
| worker 内限 1 线程, 2 workers | 65.2 |

修好后 worker 数**完美线性扩展**（8/4/2 → 15/32/65 ms）。

**端到端**：`epoch 时间 137~180 s → 68 s`（2~2.6 倍），瓶颈从数据转到 GPU。

### 13.4 排除的其它假设

| 假设 | 实测 | 结论 |
|---|---|---|
| 网络盘（Ceph）慢 | Ceph 279.4 / 本地 overlay 268.0 / **内存盘 tmpfs 272.1** ms/batch | ❌ 三者相同，与存储无关 |
| 模型太大 | 前向 13.2 ms | ❌ 模型很小 |
| 加 worker 能救 | 8/24/48 → 260/350/585 ms | ❌ 越加越差（线程超订） |
| 数据增强太重 | `imread` 占 `__getitem__` 的 83% | 部分成立，但不是主因 |

### 13.5 修复

`dataset/dataloader.py` 新增 `_worker_init`，给全部 **10 处** `DataLoader` 挂上：

```python
cv2.setNumThreads(1)
torch.set_num_threads(1)
```

**不设 seed**，因此不影响任何现有实验的随机序列。旧路径回归通过。

### 13.6 附带结论

- **不要并行跑多组训练**：cgroup 配额共享，聚合吞吐不变（实测 2 组并行各自慢 2 倍）。
- **不要靠加 worker 提速**：配额是硬上限。
- 子集实验（500 样本）天然快：**8.6 s/epoch**，42 epoch + 4 次评测约 11 分钟/臂。

---

## 14. Ntex_adapter：冻结底座、只训练 H/4 纹理适配器

> 依据 `NTEX_ADAPTER_EXECUTION_PLAN.md`。
> 动机：§12 的子集实验是"500 对从零训练整个增强网络"，必然欠训；本轮改为
> **固定全量 N0（5600 对训好的主干），只训练 29.7 万参数的 H/4 适配器**，
> 使"参考纹理有没有用"这个问题不再被欠训干扰。

### 14.1 设计

| 项 | 值 |
|---|---|
| 底座 | **全量 N0 的 ep40**（`N0_fixed_s42/model/model_00040.pt`），不是 N0_500、不是 N1、不是 init.pt |
| 可训练 | 仅 `MainNet.denoiser.ref_adapter`（A3） |
| 冻结 | estimator / embedding / encoder / bottleneck / decoder / mapping / LTE |
| 关闭 | RefIllumTransfer、RefCorrection、旧 GlobalIllumHead、TPL |
| 参考 | Nano（复用 §12 核验过的 500 个 triplet） |
| 预算 | **3000 次 optimizer 更新**；step 1–2000 LR 1e-4，2001–3000 LR 5e-5 |
| 损失 | rec 1.0 / per 0.1 / smooth 1.0 / color 0.5；exposure=tpl=adv=ref_correct=illum_match=0 |

### 14.2 计划 §7 验收清单：全部通过

| 检查 | 实测 |
|---|---|
| 参数边界 | 可训练恰为 4 个适配器张量；optimizer **1 组、297,280 参数**（与计划手算逐位一致） |
| step-0 兼容 | 开适配器（零初始化）vs 关纹理，输出差 **0.000e+00** |
| 真正的纹理输入 | A3 被调用、`T_lv3 (2,256,32,32)` 非零；`sr.requires_grad=True` |
| 梯度 | 第 0 步 `conv2.grad=8.34e-01`、`conv1.grad=0`；一步后 `conv1.grad=1.14e-01` |
| 底座固定 | **152 个冻结张量逐位未变**（训练脚本内置断言，每次运行结束自动校验） |
| 光照正确关闭 | `ref_illum` 调用 0 次；`global_illum_head is None` |
| step-0 等于 E0 | 训练开始时的评测 = **18.926 / 20.220 / 16.980**，与独立测得的 N0 逐位一致 |

计划的 §A2 准备了"若旧 checkpoint 不含适配器则另以 seed 初始化"的兜底——**实际不需要**：N0 的 checkpoint 已含适配器且 `conv2` 精确为零，可直接当 step-0 起点。

### 14.3 训练：3000 步内没有学到东西

单 batch 的 loss 噪声很大（0.16~0.47），但前后均值**没有下降**：

```
前 10 个日志均值 0.0912  ->  后 10 个均值 0.1221   (+0.0309)
训练期间的 E2 曲线(Nano 加权): 18.924 -> 18.911 -> 18.941 -> 18.894   纯噪声
```

耗时：3000 步 × 40 ms ≈ 2 分钟，加 4 次完整评测 ≈ 4.7 分钟，**训练 11 分钟**。
LR 调度按计划生效（step 2100 起 `lr=5.00e-05`）。

### 14.4 四条件评测（计划 §8）

> ⚠️ **本节数字已修正。** 第一版独立评测脚本漏设 `--ref_dir`，而 `data1.TestSet`
> 在 `ref_dir` 为空时会**静默回退到 `Ref = HR.copy()`**——所以第一版表里标着
> "Nano" 的两列实际用的是 **HR 参考**。这正好解释了后续计划 §A1 发现的
> "训练中评测 18.894 vs 独立评测 18.792" 的 0.102 dB 差异。修正后两个入口一致：
> **训练中 18.894 / 独立 18.892（差 0.002，来自 §14.6 的 ref_illum 修复）。**

两套参考设定都测，主结果是 **nano**：

**reference setting = nano（主结果）**

| camera | E0 原 N0 | E1 step3000 关 A3 | E2 正确 Nano | E3 错配 Nano |
|---|---|---|---|---|
| Huawei (30) | 20.216 | 20.216 | 20.166 | 20.241 |
| Nikon (20) | 16.981 | 16.981 | 16.979 | 16.996 |
| **0.6HW+0.4NK** | **18.922** | **18.922** | **18.892** | **18.943** |

```
E1 − E0 = +0.0000     ← 完美恢复基线
E2 − E0 = −0.0306     ← 主结果：适配器略差
E2 − E3 = −0.0514     ← 正确参考比错配参考还差
逐图 E2−E0: 均值 −0.0306  中位 −0.0515  受益 21/50  最差 −1.110
```

**reference setting = hr（诊断）**

| camera | E0 | E1 | E2 正确 HR | E3 错配 HR |
|---|---|---|---|---|
| **0.6HW+0.4NK** | **18.922** | **18.922** | **18.792** | **18.910** |

```
E1 − E0 = +0.0000     E2 − E0 = −0.1298     E2 − E3 = −0.1178
```

### 14.5 结论：当前组合下的负结果

1. **E1 精确恢复 E0（+0.0000，两套参考设定都是）**——同时证明冻结是真的、`no_ref_texture` 确实跳过适配器、拼接后处理无漂移。这是本轮最可靠的一条。
2. **E2 < E0，两套参考设定、两个相机方向一致**（Nano −0.031、HR −0.130；Huawei −0.05/−0.18、Nikon −0.002/−0.05）：训练后的适配器没有收益。**Nano 下的 −0.03 dB 已落在噪声量级**，严格说只能写"测不出增益、略偏负"。
3. **E2 < E3（Nano −0.051、HR −0.118）：正确参考比错配参考更差** → 适配器**没有学到利用对应关系**，更像一个通用残差注入；错配参考因注入更随机、平均后伤害更小。
4. **HR 参考下的损害是 Nano 的近 4 倍**（−0.130 vs −0.031）——说明适配器注入的残差在 oracle 参考下反而更"自信地错"。

**按计划 §9 的停止规则落在"明显退步 → 关闭 A3，保留 N0，归档为负结果"。**

**可能的机制（按判断排序）**

1. **冻结接收端学不会消费纹理。** N0 是在 `no_reference=True` 下训练的，F3/decoder 从未见过纹理残差。只训练适配器去喂一个没学过接收端的解码器，梯度信号本身弱。
2. **门控把信号压到 6~7 折。** `sigmoid(S)` 实测只有 0.61~0.73（§3.2 已记录）。
3. **500 对参考覆盖太窄**（48 个 pass 后更可能记住这 500 对）。
4. **LR 1e-4 对这个 297k 模块偏大**——损失全程游走、无收敛迹象。

**必须遵守的解读边界**（计划 §9 预先写明的）：

> 冻结接收端可能限制融合能力，500 对参考也限制覆盖。因此失败**不能**推广为"所有参考纹理方法无效"，只能推成"**在冻结接收端 + 当前匹配/门控 + 500 对参考 + 3000 步这个组合下无效**"。

### 14.6 顺带修复：tiled 推理的开关泄漏

准备评测时发现 `trainer._tiled_forward` 的**拼接后处理忽略 `--no_ref_illum` / `--no_global_illum`**——两个委托方法只转发、不解析开关，而拼接后是无条件调用（这正是评审 V1.1 计划时指出的 [S1]）。

修复：`TTSREnhance.illum_enabled()` 统一从 args 解析两个开关，`_tiled_forward` 按它决定是否施加后处理（没有该方法的模型保持原行为）。

**对历史结果的影响**：那些 run 的 `ref_illum` 都停在初始化（`no_ref_illum=True` ⇒ 无梯度），其输出是 ~0.001 量级的小残差，实测影响 **0.002~0.004 dB**——可忽略但确实存在。修复后 E0 从 18.924 变为 18.922。

### 14.7 产物

```
/root/data/experiments/retinex_v11/Ntex_adapter_s42/
    model/model_00000/01000/02000/03000.pt   +  adapter_*.pt
    per_image.csv        ← 逐图四条件指标
    args.txt
scripts/eval_ntex_adapter.py   ← 新增：四条件同口径评测
日志: /root/ntex_adapter.log, /root/ntex_eval.log
```

### 14.8 下一步（唯一能区分"适配器不行"与"接收端不行"的实验）

**解冻 decoder 最后几层**，其余不变，同样 3000 步、同样四条件评测。成本约 15 分钟。

- 若仍退步 → 问题在"冻结接收端 + 匹配/门控"这一层，参考纹理这条线在当前形态下可以停。
- 若转为正收益 → 说明瓶颈是接收端从未学过消费纹理，值得继续投入解冻训练。

> ✅ **已于 §15 执行完毕，结论是"仍退步"**（D1−D0 = −0.035 dB，且正确参考仍劣于错配参考）。
> 因此本节的第一个分支成立：问题在"接收端 + 匹配/门控"这一层，**当前形态可以停**。
> 详见 §15 与 §15.5 的三轮合并结论。

---

## 15. 解冻 decoder 尾部 + 纹理配对验证（D0/D1）

> 依据 `DECODER_TEXTURE_PAIRED_EXECUTION_PLAN.md`。
> 目的：§14 的 A3-only 实验无法区分"适配器不行"与"冻结接收端不行"。
> 本轮主动解冻解码器最后一段，用一个**同预算的无参考对照 D0** 来隔离"额外训练本身"的效果。

### 15.1 两臂定义

| 项 | D0 `decoder_tail` | D1 `decoder_tail_texture` |
|---|---|---|
| 底座 | 原全量 N0 ep40（SHA 前核对存在） | 完全相同 |
| 可训练 | `MainNet.denoiser.decoder_layers.1.*` + `MainNet.denoiser.mapping.*` | 同 D0，**再加** `MainNet.denoiser.ref_adapter.*` |
| 参数数 | **38,601**（17 张量） | **335,881**（21 张量） |
| 预览 | — | D1 = D0 + 297,280 |
| 参考 | 纹理路径跳过（`no_reference=True`） | 纹理开启（`no_reference=False`） |
| 关闭 | RefIllumTransfer / RefCorrection / 旧 GlobalIllumHead / TPL 全部关闭 | 相同 |
| 数据 | 同一批 500 个 Nano triplet，同一 loader（D0 也走它，只是不使用参考） | 相同 |
| 预算 | 3000 次更新；1–2000 LR 1e-5，2001–3000 LR 5e-6 | tail 同 D0，A3 为 1e-4 → 5e-5 |

**`decoder_layers.1` 的语义**（level=2 时共 2 个阶段）：H/2→H 上采样、与 H 分辨率 encoder skip 的融合卷积、该阶段的 IGAB；`mapping` 是输出投影。**未解冻** estimator / embedding / encoder / bottleneck / `decoder_layers.0` / LTE / 光照后处理。

### 15.2 启动验收（计划 §E1）全部通过

| 检查 | 实测 |
|---|---|
| D0 换参考输出不变 | **0.000e+00**（纹理路径确实跳过） |
| D1 纹理路径被调用、T_lv3 非零 | True / True |
| D1 的 A3 初始输出 | **全零**（absmax 0.000e+00） |
| RefIllumTransfer 调用次数 | **0**（普通与分块路径都是） |
| D0/D1 的 tail 起始权重逐位相同 | True |
| D1 可训练集合 = D0 集合 ∪ A3 | True |
| 梯度 | D0：tail 16 个张量非零；D1：tail 16 + A3 **2**（A3 首步只有末层有梯度，符合零初始化的预期） |
| LR 调度 | `update 2000: [1e-05]` → `update 2001: [5e-06]`；D1 `[1e-05, 0.0001]` → `[5e-06, 5e-05]` |
| 冻结校验 | **按臂重建白名单**（计划 §E2 要求），D0 记录 **139 个冻结张量逐位不变**；不再沿用上一轮写死的 152 |

### 15.3 五条件结果（step3000，计划 §F2）

**reference setting = nano（主结果）**

| camera | N0 | D0 | D1_correct | D1_a3_off | D1_mismatch |
|---|---|---|---|---|---|
| Huawei (30) | 20.216 | 20.221 | 20.164 | 20.203 | 20.230 |
| Nikon (20) | 16.981 | 16.981 | 16.979 | 16.977 | 16.991 |
| **0.6HW+0.4NK** | **18.922** | **18.925** | **18.890** | **18.913** | **18.934** |

```
Δ vs N0 :  D0 +0.0029   D1_correct −0.0324   D1_a3_off −0.0094   D1_mismatch +0.0118
Δ vs D0 :  D1_correct −0.0353   D1_a3_off −0.0124   D1_mismatch +0.0089
逐图 D0−N0:  均值 +0.0029  中位 +0.0125  受益 26/50  最差 −0.351
逐图 D1−N0:  均值 −0.0324  中位 −0.0481  受益 21/50  最差 −1.012
```

**reference setting = hr（诊断）**：D0 +0.0029、D1_correct **−0.1227**、D1_a3_off −0.0094、D1_mismatch −0.0145

### 15.4 判读（计划 §G）

1. **D0 ≈ N0（+0.003 dB）**：解冻 decoder 尾部 + mapping、用 500 对 Nano 训 3000 步，**既无改善也无损害**。+0.003 远低于 0.2~0.8 的种子噪声，只能写"测不出变化"。
2. **D1 < D0（−0.035 dB）**：在同样微调 decoder 的前提下，**加上 A3 + 参考纹理仍是负贡献**。
3. **D1_correct < D1_mismatch（−0.045 dB）**：正确参考**依然比错配参考差**，与 §14 的 A3-only 结论（E2−E3 = −0.051）方向完全一致 → **解冻 decoder 没有改变这个现象**。
4. **D1_a3_off（−0.009）优于 D1_correct（−0.032）**：关闭 A3 反而更好，指向同一结论。
5. **诊断项确认"关闭 A3 ≠ 恢复 N0"**：D1_a3_off = 18.913 vs N0 = 18.922，差 −0.009，正是 decoder 已被微调的痕迹。计划 §F2 预先写明"不要求恢复原 N0"，实测吻合。

按计划 §G：**"D1 不优于 D0/N0，或退步 → 停止本轮局部解冻方案，不自动逐层解冻、扩大数据或继续扫 LR"。**

### 15.5 三轮证据合并后的结论

| 轮次 | 实验 | 关键结果 |
|---|---|---|
| §14 | 冻结全部、只训 A3（3000 步） | E2−E0 = −0.031（Nano）/ −0.130（HR）；E2−E3 = −0.051 |
| §12 | A3 的同结构，换 Nano 训练参考 | Δadapt = −0.005（换训练参考无效） |
| §15 | 解冻 decoder 尾部 + A3，附 D0 对照 | D1−D0 = −0.035；D1_correct < D1_mismatch |

**可以下的有边界结论：**

> 在 data1 + 生成的 500 对参考 + 现有匹配（lv3 余弦 argmax）+ 现有门控（`sigmoid(S)`）+ 单点 H/4 注入这个配置下，**参考纹理分支不产生正收益**；把接收端解冻一段也没有改变这一点。

**必须遵守的解读边界**（计划 §G 预先写明）：

> 不能把有限解冻的失败等同于所有参考增强方法无效；也不能以"还有更大解冻范围"无限延长当前尝试。

**真正需要换掉的是**参考构造方式（异位置随机裁剪 + 退化）或匹配/门控机制本体，而不是继续在解冻范围、LR、更多种子上调参。当前形态已被两轮独立实验（§14、§15）否定。

### 15.6 本轮修掉的两个 bug（都是我自己引入的）

1. `main.py` 里把模块级函数 `resolve_train_arm` 当成 `Trainer` 的类属性调用 → `AttributeError`。
2. `Trainer.__init__` 的臂分支用 `mainnet.named_parameters()`（名字相对 `MainNet`，如 `denoiser.decoder_layers.1.*`）去匹配以 `MainNet.` 开头的白名单前缀 → 两条臂都报"matched no trainable parameters"。改用 `wrapped.named_parameters()`。

两次都是"类 vs 模块"和"命名空间相对谁"的同一类错误，本文件里已第三次出现——值得记住：**这个仓库的 `MainNet` 是 `TTSREnhance` 的一个子模块，`trainer.py` 的许多工具函数是模块级而非类方法。**

### 15.7 产物

```
retinex_v11/
    D0_decoder_only_s42/       model_step0000/1000/2000/3000 + adapter_*.pt + args.txt
    D1_decoder_texture_s42/    同上
    per_image_decoder_pair.csv ← 逐图五条件 × 两套参考设定
scripts/eval_decoder_texture_pair.py   ← 新增
日志: /root/decoder_pair.log, /root/decoder_pair_eval.log
```

训练各约 9 分钟（3000 步 × 70 ms + 4 次评测），五条件评测 4 分钟。

---

## 16. 至此的状态与建议

> ⚠️ **本节写于 §17 之前，其"下一步"建议已被 §17 取代，保留作为当时的推理记录。**
> 具体地：16.3 判断"应该先去修晚期回退，而不是继续调参考"。这个判断在它写下的时刻是合理的——
> 当时所有参考形态（§14/§15）都是负结果。§17 换掉整个方案后首次拿到正收益并 3 种子复现，
> 因此**参考方向重新回到主线，晚期回退降级为一个仍然存在、但适用范围更窄的问题**（见 §0.1 与 §17.5）。
> 16.1 的"已被否定"清单**依然有效**，只是要读成"被否的是那种形态，不是参考方向本身"。

### 16.1 已被否定的（不必再试）

1. **参考纹理的当前形态**：lv3 余弦 argmax 匹配 + `sigmoid(S)` 单点 H/4 注入 + 异位置随机裁剪参考 + 生成参考。
   §14（冻结接收端）与 §15（解冻 decoder 尾部）两轮独立实验都给出负结果，且**正确参考始终劣于错配参考**。
2. **用换训练参考来救活参考光照分支**（§12，Δadapt = −0.005）。
3. **靠调解冻范围 / LR / 更多种子来救当前形态**——计划 §G 预先写明，我也同意。

### 16.2 仍未验证、但优先级也不高的

- 同容量常量参考训练对照（用于把 D1−D0 里的"额外容量"剥离出来）。只有当某个形态先出现正收益时才值得做。
  → **§17 已经用更干净的方式回答了这个问题**：同容量 V2-Self 对照 3/3 为负，说明增益确实来自参考内容。
- 更强的匹配机制（top-k 软迁移、双向一致性），以及真正**改掉参考构造方式**（例如同位置裁剪做 oracle 上限测试）。§15.5 指出这才是唯一还没试过的方向。
  → **§17 就是这个方向的第一步**（换掉参考构造 + query + 特征空间 + 匹配 + 融合位置），并且拿到了正收益。
    top-k 软迁移、多尺度、HR 参考上限仍未做。

### 16.3 建议的下一步：修晚期回退，而不是继续调参考（⚠️ 已被 §17 取代）

**理由**：晚期回退横跨所有实验——四组全量、N0/N1、三组子集、D0/D1 都在 ep30/40 回退，**包括完全没有参考分支的 N0_500**。它决定了任何结论的噪声底（±0.5 dB），比参考分支那 0.03~0.13 dB 的效应量大一个数量级。

**已知的候选原因**（§4.2、§9）：

1. `exposure_w` 与重建目标冲突（已修，收益 +0.67 dB，但没消除回退）
2. 缺 `--decay`（已修）
3. **仍未定位的部分**：为什么连单调上升的 N0 也会在 ep30 停住

**最小验证方案**：用 N0 的配置、`exposure_w=0`、`decay 20`，跑到 60~80 epoch，看曲线是继续上升还是继续在 ep30-40 后回退。若仍回退，则说明问题不在配方而在数据（500/5600 规模下的过拟合或标注噪声），需要换方向诊断。

---

## 17. V2：放弃 H/4 单点注入，改成"输出端局部参考细化"（首次正收益，3 种子复现）

> 计划：`/root/projects/V2_LOCAL_REFERENCE_REFINEMENT_PLAN.md`（V2.0）
> 完整小结：`/root/data/experiments/retinex_v2_localref/findings_v2_localref.md`

### 17.1 为什么换方案，而不是接着调 A3

§14/§15 已两轮独立否定旧形态（lv3 余弦 argmax + `sigmoid(S)` 单点 H/4 注入），且**正确参考始终劣于错配参考**。
所以本轮**不**在旧形态上继续调 LR / 解冻范围 / 参考质量，而是整体换掉：参考构造、query 位置、特征空间、匹配方式、融合位置全变。

底座仍然是原全量 N0（ep40，`sha256 = 777c1dce…`），**完全冻结**；只训练一个 78,724 参数的新模块，
接收 `Y0 = infer_n0(X)` 与第二路输入（Self：Y0 本身；Nano：生成参考 R），输出 `sr = Y0 + gate * Δrgb`。

### 17.2 结果（step 3000，PSNR-rgb，0.6HW+0.4NK）

| 条件 | Huawei | Nikon | 加权 | ΔvsN0 | ΔvsSelf |
|---|---:|---:|---:|---:|---:|
| N0 | 20.2161 | 16.9813 | 18.9222 | 0 | — |
| V2-Self | 20.1729 | 16.9869 | 18.8985 | −0.0237 | 0 |
| **V2-Nano-correct** | **20.3468** | **17.1180** | **19.0553** | **+0.1331** | **+0.1568** |
| V2-Nano-bypass | 20.2161 | 16.9813 | 18.9222 | **+0.0000** | — |
| V2-Nano-mismatch（诊断） | 19.6333 | 16.6567 | 18.4427 | −0.4795 | — |

`SSIM(rgb)`：N0 0.6250 / Self 0.6248 / **Nano 0.6258** / mismatch 0.6206。
逐图（Nano vs N0）：mean +0.1331，median +0.1275，**34/50 受益**，最差 −1.550，最好 +1.234。
训练轨迹 Nano 单调上升、Self 单调轻微下降，两者方向相反且各自单调（不是噪声里交叉）。

### 17.3 为什么这次认为是"参考在起作用"

1. **同容量 Self 对照不给增益**（−0.0237）：两组 78,724 参数、同一 `refiner_init.pt`、同数据同预算，唯一差别是第二路输入。
   → +0.1331 **不是**"多加了一个 CNN"。
2. **错配参考大幅退化**（correct − mismatch = **+0.6126 dB**）：模块确实在读参考内容。
3. **gate 变成空间门**：末步 `gate ∈ [0.032, 0.582]`，不再是标量式注入。

### 17.4 复核：换种子重跑（seed 7 / 123）—— 已通过

每个种子用**自己的初始化**（`refiner_init_s{seed}.pt`；同种子的两臂仍共享同一个），
因此同时改变了初始化与数据顺序，而不是只重测一遍数据顺序。

| seed | V2-Self | V2-Nano | Nano−N0 | Nano−Self | correct−mismatch |
|---|---:|---:|---:|---:|---:|
| 42 | 18.8985 | 19.0553 | **+0.1331** | **+0.1568** | +0.6126 |
| 7 | 18.9051 | 19.0516 | **+0.1294** | **+0.1465** | +0.6207 |
| 123 | 18.9042 | 19.0411 | **+0.1189** | **+0.1368** | +0.4513 |

```text
Δscheme   mean +0.1272  sd 0.0074  range [+0.1189, +0.1331]   3/3 为正
Δexternal mean +0.1467  sd 0.0100  range [+0.1368, +0.1568]   3/3 为正
correct − mismatch      mean +0.5616                          3/3 为正
SSIM(Nano)−SSIM(N0)     +0.0004 ~ +0.0009                     3/3 为正
Self − N0  始终为负（seed42/7/123 = −0.0237 / −0.0170 / −0.0179）；bypass − N0 3/3 精确为 0
```

**种子间极差（0.014~0.020 dB）比效应量（0.127 dB）小一个数量级，信噪比 ≈ 17。**
所以 §17.2 的正收益**不是 seed 42 走运**：符号、量级、"Self 为负"、"mismatch 退化"四条证据在 3 个种子上都复现。

### 17.5 边界（复核之后仍然成立）

- 复核只排除了**初始化/数据顺序**的偶然，**没有**排除评测集偶然：三次都是同一批 50 张开发图。
  → 仍不能写成"涨点方法"，只能说"多种子可复现的阳性候选"。
- **尾部风险被复现且略变差**：逐种子最差 −1.550 / −2.063 / −1.693 dB；50 张里 9 张在不同种子间正负翻转。
  均值稳定 ≠ 每张都赢。seed 123 的 Nikon 只有 +0.073，是 6 个设备×种子单元里最弱的。
- Nano 是**生成**参考；本轮不涉及 HR 参考上限（§11 的 HR +0.76 是另一件事）。
- **本轮的晚期回退问题不适用**（底座冻结、只训小模块、按 step 而非 epoch）；旧路的 ep30 回退仍独立未决。
- 与旧 A3 无关：不能拿本轮给 §14/§15 翻案，也不能把本轮整体增益拆给单个模块。

### 17.6 下一步

下一关**不是**再调结构，而是 **换评测数据**（或至少先分析那批退步样本的 gate 热图与参考图）。
在拿到独立数据之前，不扩大投入；不打开旧 A3 / RefIllumTransfer / decoder 解冻（§14/§15 已否定）。

> **工程勘误**：本轮两臂实测各 ≈ 10.3 分钟、合计 ≈ 26 分钟，而此前那份计划按 1.8 s/step 推出的"半天"高估了约 9 倍
> （真实步时 ≈ 0.19 s = compute 0.056 + dataloader 0.132）。以后估时先量一次 `s/step`。

---

## 18. V2.1 计划的前置实测核对（2026-09-24）

> 被核对的计划：`/root/projects/V2_1_DATA_CACHE_REVALIDATION_PLAN.md`
> 被核对的代码审查：`/root/projects/REVIEW_V2_8ac9f4.md`
> 完整核对报告：`/root/projects/V2_1_PREFLIGHT_AUDIT.md`
> 机读产物：`/root/data/experiments/retinex_v2_localref/audit/data_preflight.json`

只读核对（不重训、不改代码），结论如下。

### 18.1 审查提出的三个修复，实测影响各不相同

| 问题 | 代码是否属实 | 在已运行的 V2 实验里的实际影响 |
|---|---|---|
| P0 缩放后归一化失效 | ✅ 属实 | **训练 0/500 触发**（训练 Nano 与 low 同尺寸）。评测 50/50 需要 resize，但评测走的是先归一化后缩放的正确路径 → **本轮未触发** |
| P1 `_epoch` 不更新 | ✅ 属实 | 每个样本在全部 ≈48.4 趟里用**同一份 crop/rot/flip**；有效增强只有 500 个固定裁剪。**唯一实质改变训练的一项** |
| P1 分块外圈零权重 | ✅ 属实 | 缓存最外 1px 环 100% 精确为 0，但只占 ΔSSE 的 **2.8%**；去掉外圈后 Δ 仍为 +0.1084（全图 +0.1093，未 round 口径）→ **不是增益来源** |
| P2 SSIM 实为 Y 通道 / `base_sha` 记成 refiner init | ✅ 属实 | 命名与身份记录错误，不影响 PSNR 数值 |

按计划 §A2 自己定的规则，P0 应记为"**防御性修复，本轮未触发**"，不能用来质疑旧分数。

### 18.2 新发现（两份文档都没写）

1. **训练/评测的参考分辨率不一致**：训练 ref 与 low 完全同尺寸；
   评测 ref 是 1195×896 / 1264×843（比 low 大 1.24× / 1.32×），靠 bilinear 下采样回到 low 尺寸，
   且未开 antialias。refiner 在"原生 low 尺寸参考"上训练、在"放大后再缩小的参考"上评测。
2. **`sample_id` 不是全局唯一键**：`Nikon/3004.jpg`、`Nikon/3017.jpg` 在 train 与 eval 下同名。
   已核 md5，**内容不同，不是泄漏**；但说明 `camera/filename` 不能当跨 split 主键。
3. 指标对 round 敏感：同一批张量未 round 的 Δ = +0.1093，官方 round 后 = +0.1331（已精确复现无误）。

### 18.3 本节的更正

- §17.4 的 Self−N0 原写作"−0.0237 / −0.0179 / −0.0242"，**算式笔误**。
  按 `per_image_v2*.csv` 核对后应为 **−0.0237 / −0.0170 / −0.0179**（seed 42 / 7 / 123）。已改，方向不变。
- `summarize_local_refine_seeds.py` 的默认 glob `per_image_v2*.csv` 会误匹配将来的 `per_image_v21_*.csv`，
  以后再跑汇总前需显式指定协议前缀。

### 18.4 对下一步的含义

三个修复预期都**不会推翻** §17 的正收益（一个未触发、一个只占 2.8%、一个只是让训练信号更充分）。
因此真正有价值的下一关仍是**独立数据**（§17.5）：同一 50 张开发图反复复核仍属开发集。
修复值得做（清理代码、顺带看增强多样性是否放大效应），但不要指望它产生新的科学结论。

---

## 19. 按 §18 修复四处 bug（代码 + 测试；未重训）

> 范围：只改代码与测试，**没有重训、没有重新生成缓存、没有改模型结构 / 损失 / LR / 3000 步预算**。
> 因此 §17 的 3 种子结果仍然只属于**旧协议**，本节不产生新分数。

### 19.1 改了什么

| # | 文件 | 修改 | 性质 |
|---|---|---|---|
| 1 | `dataset/data1_localrefine.py` | 新增 `read_rgb` / `raster_to_model_tensor` / `resize_tensor` / `read_rgb_model_tensor`，固定 **解码 → 归一化 → resize → 几何** 顺序；数值映射只看源 dtype，未知 dtype 直接报错 | **防御性**（训练 0/500 未触发，但换数据立刻会踩） |
| 2 | `dataset/data1_localrefine.py` | 新增 `LocalRefineTrainSet.set_data_pass()`；删除**未被引用**的 `LocalRefineEvalSet`（它自带一份同款错误顺序） | **实质**：此前每样本在整个 run 里只有一份固定 crop |
| 3 | `trainer.py` | `_tiled_forward` 重写窗口：按轴取 `min(tile,H)`、`tile_starts()` 保证起点非负且末块贴边、**只对真有邻块的内部接缝做渐变**（图最外侧保持权重 1）、`overlap=1` 用非零 ramp、除法前断言权重有限且全为正 | 修掉外圈 1px 恒为 0 |
| 4 | `local_refine_runtime.py` / `scripts/eval_local_refine.py` / `scripts/train_local_refine.py` / `scripts/summarize_local_refine_seeds.py` | SSIM 改标 `ssim_y`；CSV 记录真实 `base_checkpoint_sha256` + `refiner_init_sha256` + `checkpoint_sha` + `cache_metadata_sha256`；底座加载失败即报错；run 目录已有产物时拒绝覆盖（`--allow_overwrite` 可显式放开）；缓存读入校验完成标记；汇总默认只匹配本协议 CSV | 记录与防护 |

配套：`scripts/prepare_local_refine_cache.py` 现在写入 `preprocess_version`、`inference_code_sha256`、`repo_commit`、manifest 哈希、输入指纹与 `complete` 标记；
新增 `tests/test_v21_data_cache.py`（9 项，CPU，可独立运行）。

### 19.2 验证

```text
tests/test_v21_data_cache.py   9/9 通过
  - 常量 128 灰度图 resize 前后都 ≈0.0039216（旧代码在 resize 后变成 128）
  - float32 栅格被拒绝，不再猜范围
  - 真实评测参考：训练读取路径 == 评测读取路径（max|Δ| < 1e-6）
  - 同一 idx 跨 data pass 产生不同几何；同 (seed, pass, idx) 精确可复现
  - 恒等模型 × (128×128, 256×256, 400×600, 128×400, 400×128, 255×257) × overlap(0,1,96)：
    形状正确、全图正权重、恒等误差 ≤ 1e-6
  - 720×960 外圈不再是 0（旧实现最外 1px 环 100% 为 0）
  - overlap=1 接缝不再被清零
  - 新增候选未通过时：越界/NaN 直接抛错，不再用分母 clamp 掩盖
```

端到端冒烟（临时根目录 `/tmp/v21_smoke`，5 步，未触碰真实结果）：
两臂各跑通、step0 恒等成立、五条件评测与新版逐图 CSV 正常产出，
legacy 缓存被正确标记为 "no completion flag"。

### 19.3 必须注意的后果

1. **现有 `cache_n0_train` / `cache_n0_eval` 已失效。** 它们是用旧的、外圈恒为 0 的窗口生成的。
   修复后重跑任何训练/评测前必须重新生成缓存（新目录，不要原地覆盖）。
   在此之前，所有依赖旧缓存的数字（含 §17 的 18.9222 基线）都属于**旧协议**。
2. **新 N0 基线必然与 18.9222 不同**（外圈 1px 从 0 变为正常值）。这是工程变化，不是涨点。
3. §17 的三个种子结果**没有被推翻**：修的是"以后别踩坑"，不是"旧结果算错了"——
   归一化在训练里未触发，分块只占 ΔSSE 的 2.8%（§18）。
4. 唯一可能**放大**效应的是第 2 项（增强多样性）：旧的有效训练集只有 500 个固定 crop。

### 19.4 下一步（尚未执行）

按 V2.1 计划：重新生成 Y0 缓存 → 跑 tiling 恒等矩阵 → seed42 的 Self/Nano 各 3000 步 → 同口径五条件对照。
总时长估计 ≈ 30 分钟（两臂 ≈ 21 分钟 + 缓存 ≈ 3 分钟 + 评测 ≈ 2 分钟）。

---

## 20. V2.1 重评结果：修复后信号仍在，但幅度变小

> 新根目录：`/root/data/experiments/retinex_v21_verified/`（旧根目录未改动）
> 完整报告：`/root/data/experiments/retinex_v21_verified/findings_v21.md`
> 耗时 ≈ 25 分钟（缓存 3 + 两臂各 10 + 终评 1.5）。旧缓存与旧结果全部保留。

### 20.1 结果

| 条件 | Huawei | Nikon | 0.6HW+0.4NK | ΔvsN0 | ΔvsSelf |
|---|---:|---:|---:|---:|---:|
| N0-fixed | 20.3644 | 17.0712 | 19.0471 | 0 | — |
| V2-Self-fixed | 20.3308 | 17.0985 | 19.0379 | −0.0092 | 0 |
| **V2-Nano-fixed-correct** | **20.4657** | **17.1620** | **19.1442** | **+0.0971** | **+0.1063** |
| V2-Nano-fixed-bypass | 20.3644 | 17.0712 | 19.0471 | **+0.0000** | — |
| V2-Nano-fixed-mismatch | 20.0442 | 17.1077 | 18.8696 | −0.1775 | — |

逐图（Nano vs N0）：median +0.1117，**34/50 受益**，最差 −1.244，下降超 1 dB 仅 **1 张**；
分设备 Huawei +0.1013（21/30）、Nikon +0.0908（13/20），**2/2 为正**。
`SSIM(Y)`：N0 0.6251 / Nano 0.6257。bypass 精确等于 N0，step0 恒等仍成立。

按 V2.1 计划 §G2 第一档判定 → **保留候选**；下一关是独立数据。

### 20.2 与旧协议的对照，以及"修复没有放大效应"

```text
               旧协议(seed42)   新协议(V2.1, seed42)
N0                18.9222          19.0471     ← +0.1249（工程变化）
Δscheme           +0.1331          +0.0971
Δexternal         +0.1568          +0.1063
correct−mismatch  +0.6126          +0.2747
```

用"旧权重跑新缓存"的探针把变化拆开：

```text
旧缓存 + 旧权重  +0.1331
新缓存 + 旧权重  +0.1293    （缓存修复：−0.0038，可忽略）
新缓存 + 新权重  +0.0971    （重新训练：−0.0322）
```

**所以：缓存修复抬高了基线水平但不改变差值；差值变小来自重新训练这一步。**
§18.4 曾预期"增强多样性可能放大效应"——实测**没有放大，反而略减**。
可能机制是增强更充分后两臂都变好，相对优势缩小；同时 `correct−mismatch` 从 0.61 降到 0.27，
说明修复后模型对参考内容的依赖变弱了。本轮只有 1 个新种子，这 0.032 dB 不宜过度解读，
但它比旧协议种子间 sd（0.007）大，不能当作零。

### 20.3 一处必须更正的先前估计

§18 与审计报告 §2.3 曾写"边缘伪影最多只能贡献 0.087 dB"。那是**未 round、合并 SSE** 口径
（边缘占 1.99%）。换到主指标的**逐图 round** 口径，旧缓存那圈 1px 实际承担每图 SSE 的 **3.34%**
（约平均像素的 7 倍误差），修好它把 N0 抬高 **+0.1249 dB**。

两个结论并不冲突：边缘抬高的是 N0 的**水平**，不是 Nano−N0 的**差值**。
但当初那句"最多 0.087"是**错的**，教训是区域贡献必须用与主指标相同的 round/聚合口径计算。

### 20.4 新增的一条观察

新协议下 Nano 在 step2000 见顶（19.1617）、step3000 回落（19.1442，**−0.0175**）。
幅度小，但这是"晚期回退"在冻结底座协议下第一次露头，记录备查。

### 20.5 边界

- **不要**把 V2.1 与旧 seed7/123 合并成"修复后 3 种子"（计划 §G2 明令禁止）。
- 新 N0（19.0471）与旧 N0（18.9222）不可互换引用。
- 仍是同一批 50 张开发图 → 开发集筛选，不是独立泛化证据。
